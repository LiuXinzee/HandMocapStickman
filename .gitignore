# Dependencies
**/node_modules
.pnpm-store/

# Build outputs
dist/
build/
*.dist

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Database
*.db
*.sqlite
*.sqlite3

# Webdev artifacts (checkpoint zips, migrations, etc.)
.webdev/

# Manus version file (auto-generated, not part of source)
client/public/__manus__/version.json

# Local workspace artifacts and the untouched legacy project snapshot
.manus-logs/
hand_mocap_stickman/
.project-config.json

# Python 本地虚拟环境与字节码（python_train / glove_bridge 用）
.venv/
__pycache__/
*.pyc

# 手工备份文件，不入库
*.bak-*

# 训练数据集导出（python_train/data/：从浏览器 IndexedDB 导出的 dataset.bin/json，43MB）
# 与 keras 模型产物（python_train/out/）**入库**。
#
# 原本忽略了这两个目录，理由是"构建输入/输出不是源码"。改成入库的理由更重：
# dataset.bin 是 IndexedDB 的**唯一备份**，忽略它就等于这份 399 条录制只存在于
# 本机一块磁盘上，删了没有撤销。异地备份的价值大于仓库变大的代价。
#
# ⚠ 代价是真的：dataset.bin 是二进制，git 存不了增量，**每次重新导出并提交都会
# 再加约 43MB 到仓库历史里，且永久不可回收**（除非 filter-repo 重写历史）。
# 所以：采集完一批再导出一次，不要每改一点就提交。单文件 100MB 是 GitHub 的硬上限，
# 现在 43MB，还能再涨一倍多；到那时候要转 git-lfs。

# 例外：句子模型的**权重**要随代码部署（浏览器运行时从 /models/ 读），必须入库。
# 只有 623KB，是 export_weights.py 从 out/ 里抽出来的推理所需部分
!client/public/models/
