# HandMocapStickman 算法技术剖析

**文档性质**：面向技术评审的算法说明。所有维度、超参数、阈值与实测指标均取自源码与实验记录，可逐条溯源；设计文档中的目标值不予采用。

**版本**：2026-09-14

---

## 1. 系统定位与已实现指标

本项目是一套**传感受限条件下的连续手语识别系统**。输入为一对集成压力阵列、弯折传感与六轴 IMU 的数据手套；输出为汉语词序列及语序重排后的自然语句。训练阶段在 Keras 完成，推理阶段在浏览器端 TensorFlow.js 完成，两侧网络结构逐层同构。

| 识别链路 | 网络结构 | 数据规模 | 评测指标 |
| --- | --- | --- | --- |
| 孤立词（2000 ms 滑动窗口，T=32） | TCN + 全局平均池化头 + 交叉熵 | 439 条序列录制，27 类 | 验证集准确率 **97.7%**（按录制分组划分） |
| 连续语句（整句捕获，T=128） | TCN + 逐帧分类头 + CTC | 1770 条（真人 90 / 合成 1680），26 类 + blank | 合成验证集 WER **0.7%**（效力范围见 §9） |
| 触觉→骨架回归 | MLP，141D → 21×3 | 单帧样本库 | 遗留链路，深度通道存在结构性缺陷 |

系统的技术价值集中在三个方面：

1. **可溯源的超参数体系**。每一项阈值与架构决策均对应一次测量或一组对照实验，并记录有效样本量与适用边界。
2. **可识别性判定前置**。在多处场景中先行判定「目标信息是否存在于传感通道中」，据此区分模型容量不足与观测不足，并对后者采用物理判据或类别空间重构而非继续扩充数据。
3. **面向静默失效的防御设计**。系统性识别并封堵「编译通过、运行正常、指标合理但结论错误」这一类缺陷。

---

## 2. 系统架构

```
双手套 Web Serial（921600 bps）
  └─ packet-2 数据段长度在线探测（144 B / 168 B → 272 B / 296 B 帧）
      └─ 256 原始通道 → 137 按物理位置重映射通道（60 指压 + 5 弯折 + 72 掌压）+ IMU 四元数
          └─ 50 Hz 公共时间栅格（左右手为独立串口，需先行时间对齐）
              ├─ 孤立词链路：2000 ms 窗口 → 运动门控 → 主手判定 → 镜像归一化
              │                 → T=32 重采样 → 交叉熵分类 → 连续一致性与置信度确认
              │                 → 转角/拇指仲裁 → 复合词重组 → 输出
              └─ 语句链路：起手检测 → 整句捕获 → 自适应收句 → T=128 重采样
                              → CTC 贪心解码 → 逐词区间仲裁 → 复合词重组
                              → 屏蔽表过滤 → 合并类句位消解 → 语序重排 → 语音合成
训练期辅助通道：单目摄像头 + MediaPipe 21 关键点（仅用于教师模型输入与样本裁剪判据，不进入部署特征）
```

---

## 3. 特征表示：以设计保证不变性

每手 **147 维/帧**；双手拼接构成学生模型输入 294 维；教师模型额外拼接归一化关键点 63×2，共 420 维。

| 维度区间 | 内容 | 设计依据 |
| --- | --- | --- |
| `[0:137]` | 传感通道值 / 255 | 按物理位置重映射，非原始字节序 |
| `[137:141]` | 相对首帧四元数 `q₀⁻¹ ⊗ qₜ` | 消除绝对朝向依赖 |
| `[141:144]` | 重力方向在手系的投影 `R(q)⁻¹·[0,0,1]` | 对绕重力轴旋转天然不变 |
| `[144:147]` | 加速度 / 16，截断至 [−1,1] | 本批硬件该槽位实测恒零，当前不携带信息 |

### 3.1 偏航不变性的设计取舍

两条 IMU 派生通路均对偏航（yaw）不变，且为主动设计。手套采用 ICM-42688 六轴方案，无磁力计，绝对偏航不存在零点，上电偏置随机且持续漂移。若将该量纳入特征，模型需拟合一个每次上电均不同的随机偏置。

该取舍的代价经定量评估。`yawDrift.ts` 为此专门实现了一个**只读可行性探针**，在既有数据上度量两项此前从未测量的量：

- 静止状态偏航漂移率 P90 为 **15.92 °/s**，10 s 累计偏差 159°；
- 目标类别（「你」/「他」）的类间朝向距离约 **45°**；
- 静止基准位的零点重复性 P90 为 **43.4°**（该项包含受试者每次复位的姿态差异，与陀螺漂移为独立误差源）。

结论：为偏航建立体坐标系参考的方案，累计误差超出类间距离 3 倍以上，**不具备可行性**。据此，相关类别在标签空间层面合并（§7），而非继续优化模型。

> 方法论要点：功能开发前先以既有数据完成可行性判定（go / no-go）。探针模块严格只读，不写入任何持久化存储。

### 3.2 关键点归一化（教师模型）

手腕点（landmark 0）保留绝对坐标以承载运动轨迹；其余 20 点先作手腕平移不变化，再除以手长 `‖lm9 − lm0‖` 作尺度归一化，使该通道承载手形而非画面位置。遗留的静态单帧链路未执行此步骤，导致教师模型将画面位置作为判别特征并经蒸馏传递至学生模型；该缺陷已在文档中明确标注为待修项。

---

## 4. 骨干网络

```
输入 [T, D]
  b1: Conv1D(c1, k=5, padding=same, use_bias=False) → BatchNorm → ReLU      T
  MaxPool1D(2)                                                              T/2
  b2: Conv1D(c1, k=5) → BatchNorm → ReLU                                    T/2
  MaxPool1D(2)                                                              T/4
  b3: Conv1D(c2, k=5) → BatchNorm → ReLU                                    T/4
  [可选] Bidirectional(GRU(c2/2, return_sequences=True))
通道配置：教师 c1=128, c2=256；学生 c1=64, c2=128
有效感受野：5 → 6 → 14 → 16 → 32 帧
```

时间维仅在分类头内塌缩，骨干输出保持 `(T', C)` 形状。孤立词链路后接全局平均池化，语句链路后接逐帧分类头，**更换分类头不更换骨干**。该性质是跨链路权重复用（§5.2）的结构前提。

### 4.1 由工具链约束驱动的架构重设计

初始版本采用空洞卷积（dilated convolution）实现长感受野。TensorFlow.js 4.22 的 `conv2D` 前向计算支持 dilation，**反向传播不支持**。其表现为：模型可构建、可执行 `predict`、`tsc` 与 `build` 全部通过，仅在实际执行 `fit` 时抛出异常——浏览器端训练页面从未成功运行，且异常表征不指向卷积配置。

解决方案为以「小卷积核 + 池化」堆叠出等价的 32 帧感受野，并新增一条执行**单步真实 `fit`** 的单元测试（`sequenceModel.test.ts`）锁定该失效模式，防止回归。

---

## 5. 分类头、损失函数与跨链路迁移

### 5.1 两个分类头

**孤立词头**：GAP → Dense(hidden, ReLU) → Dropout → Dense(num_classes, softmax)，交叉熵损失。

**语句头**：`Dense(num_classes + 1, softmax)`，逐帧输出，不作时间塌缩，blank 位于末位下标。

CTC 损失的实现如下：

```python
tf.nn.ctc_loss(labels=...,
               logits=tf.math.log(tf.maximum(y_pred, 1e-12)),
               logits_time_major=False,
               blank_index=-1)
```

由于分类头自带 softmax，无法取得 logits。此处依据恒等式 `log_softmax(log p) = log p`（因 `log pᵢ = zᵢ − logsumexp(z)`，常数项在 log_softmax 中消去）完成等价改写。代码中标注该式为数学等价，禁止「还原」为直接传入 logits。

**时间分辨率约束经解析求解后确定**。骨干两次池化使输出帧数为 `T/4`；CTC 要求输出帧数不小于 `2L − 1`（存在重复标签时）。`T=32` 仅得 8 个输出帧，对 4 词语句已不可靠。故语句链路取 `T=128`，得 32 个输出帧，约 5 帧/词。

`build_ctc_xy` 中设有主动终止断言：

```python
if seq_len // 4 < max(2 * len(t) - 1 for t in targets):
    raise SystemExit(...)
```

依据为：该条件不满足时 `tf.nn.ctc_loss` 不抛异常，而是**静默返回 inf**。此处取「训练启动失败」优先于「产出损失为 NaN 的模型」。

### 5.2 解码与评测口径

`ctcDecode.ts` 与 `python_train/ctc_decode.py` 逐语句对应。贪心解码核心：

```ts
if (best !== prev && best !== blank) out.push(best);
prev = best;              // blank 帧同样更新 prev
```

执行顺序为**先折叠连续重复、再剔除 blank**，且 `prev` 记录折叠前的 argmax。顺序颠倒会将 `[a, blank, a]` 折叠为单个 a——以 blank 分隔重复标签是 CTC 的核心机制。两侧约定不一致时不产生异常，仅表现为「浏览器解码结果与训练期报告的 WER 不一致」，而 WER 是唯一验收指标。为此 `ctcDecode.test.ts` 使用**由 Python 导出的 logits 固定样本**逐位断言。

**语句级评测仅采用 WER**（Levenshtein 距离 / 参考长度之和），不采用逐帧准确率：32 个输出帧对应 3~6 个词，一个恒输出 blank 的退化模型逐帧准确率即可达 85% 以上。

### 5.3 骨干迁移

语句模型以孤立词学生模型的**同名同形层**初始化，仅跳过 `*_out` 输出层（`transfer_backbone`，按层名与权重形状精确匹配，不匹配则跳过并计数上报）。

该步骤为收敛前提而非可选优化：CTC 需同时学习类别判别与时间定位，梯度稀疏度显著高于分类任务，在 90 条真人语句规模下随机初始化不收敛。卷积与批归一化参数与输入长度无关，故 T=32 训得的权重可直接用于 T=128——此即 §4 中「骨干输出不塌缩时间维」设计所换取的结构收益。

---

## 6. 跨模态知识蒸馏

```
阶段一  以融合特征（触觉 294 + 关键点 126 = 420 维）训练教师模型
阶段二  soft   = softmax(log(p) / T),        T = 3
        target = α·soft + (1 − α)·hard,      α = 0.5
        以 target 训练纯触觉学生模型（输入 294 维，不含视觉通道）
门控    数据集视觉覆盖率 < 80% → 教师训练与蒸馏整体跳过，学生直接使用硬标签
```

三项实现要点：

- `softmax(log(p)/T)` 与对 logits 作温度缩放严格等价，依据同 §5.1。
- **验证集一律使用硬标签**。以混合标签计算的验证准确率度量的是学生对教师的拟合程度，而非分类正确性。
- **覆盖率门控**：视觉缺失比例过高时教师模型本身不可信，蒸馏会将噪声作为知识传递。此时退化为标准监督训练，而非按可用比例部分蒸馏。

该范式由部署约束反向确定：摄像头仅存在于采集与训练阶段，推理页面不依赖摄像头。

---

## 7. 类别空间设计：以通道可识别性为准则

`labelMerge.ts` 对特征层不可分的类别执行合并映射：

- `merged_pron_sg` = {你, 他}，默认成员 `you`；
- `merged_pron_pl` = {我们, 你们, 他们}，默认成员 `they`，自指成员 `we`。

合并准则为「差异由哪个通道承载」，而非语义相近性：

- 「你」/「他」的差异仅为绕重力轴的水平角，而六轴 IMU 无绝对偏航，特征层**不可分**（定量依据见 §3.1）；
- 「我」**单独成类**：指向自身胸口会产生真实的压力接触事件，且俯仰/滚转角亦不同，在特征空间中可分。其直接效果是「我爱你」与「你爱我」构成两个不同的解码序列；
- 复数组**有意保留「我们」**，因该类别训练数据为零。该不对称性为主动设计并在代码中说明。

两项工程约束：

- 合并仅为训练期的标签重映射，持久化存储始终保留原始标签，更换九轴 IMU 后可无损撤销；
- `defaultMember` 必须显式声明，不得退化为 `members[0]`（该处曾发生一次静默回归）。

---

## 8. 推理侧判据链

### 8.1 运动门控（开集拒识的替代方案）

闭集分类器不具备「非目标类」输出，而滑动窗口中绝大多数窗口不含手语。此处的关键观察是：**静止状态是置信度最高的误报来源**。手部静止时相邻窗口内容近乎一致，「连续 N 窗结论相同」必然满足；常量输入上的 softmax 分布尖锐，置信度门限亦必然满足。

原设计以 `_idle` 伪类承担该职责，实测该类仅 4 条样本、时长中位数 211 ms，相对 2000 ms 窗口不具备覆盖能力。

现方案为在推理前加入物理门限：`peak = max(左手能量, 右手能量) ≥ IDLE_ENERGY`。阈值**复用主手判定已有的能量口径**，避免引入第二套独立阈值。在全部 399 条真实录制上验证：低于门限者 7 条（4 条 `_idle` + 3 条确认空录），392 条真实手势全部高于门限。

设计上不设滞回（漏判一个采样周期的代价为 100 ms 延迟，误判一个采样周期的代价为一个错误词），且不应用于单帧路径。输入必须为**镜像归一化前**的快照，因能量需除以该手自身标定量程。

### 8.2 四元数累计路径转角速率

区分「谢谢」（手部近乎静止，仅拇指下压）与「难过」（掌心朝胸口画圆，腕部朝向持续变化）。两种算法在 348 个 2000 ms 窗口上的对照：

| 判据 | 最优阈值 | 分类正确率 |
| --- | --- | --- |
| 相对首帧的最大转角 | 69.2° | 71.0% |
| **累计路径转角** | 112.6° | **97.7%** |

差异来源于轨迹闭合性：画圆动作终点回到起点，端点夹角小而路径弧长大。故采用逐帧相对旋转角之和。

实现要点：逐帧角度取 `2·acos(min(1, |rel[0]|))`，标量部分取**绝对值**——四元数 q 与 −q 表示同一旋转而 IMU 输出符号随机跳变，不取绝对值将在每次符号跳变处累计 180°，静止手部可读出数千度。

采用角速率而非累计角度：语句链路中各词的 CTC 区间长度在 200 ms ~ 3 s 之间，累计角度随区间长度线性缩放，短词将被系统性判为静止。

**适用边界经全窗长扫描确定**：

| 窗长 | 拇指判据 | 转角速率判据 |
| --- | --- | --- |
| 800 ms | 84.0% | 77.8% |
| 1200 ms | 88.3% | 85.9% |
| 1500 ms | 88.0% | 94.8% |
| 2000 ms | 88.2% | 97.7% |

短区间下新判据劣于原判据。成因为「谢谢」的转动呈阵发特征（抬手到位与收手离场各有一次急动），短窗口易恰好截取该段，读出与画圆相当的速率。据此导出 `ROT_MIN_SPAN_MS = 1500`，短于该值时读数置为不可用并回退至拇指判据。其推论是：该判据在语句链路上多数情况处于关闭状态（语句中单词区间通常为 750~1500 ms），此为有意设计并在代码中说明。

阈值采用三段式：≥70 °/s 判为转动（该侧无假阳性，193 个「谢谢」窗口无一到达）；<50 °/s 判为静止（该侧存在 22.4% 的「难过」窗口混入）；中间带 [50,70) 占 16%，交由拇指判据仲裁。

选择 50/70 而非正确率略高的 40/70（98.0%），依据为后者将 28% 的判定转移至可靠性存疑的拇指通道。并给出了该通道完全失效条件下的鲁棒性对照（拇指读数恒为 <5 时，三段式 50/70 仍达 92.0%，仅用拇指判据为 44.5%）。

**有效样本量声明**：348 个窗口来自 30 条录制，窗口步进 100 ms、窗长 2000 ms，相邻窗口重叠 95%，有效样本量接近 30。两项阈值标注为暂定值，需经实时读数校准。

### 8.3 拇指压力判据

判据取拇指 12 个压力点的**单点峰值**而非 12 点均值：

- 12 点均值口径：谢谢 1.25 vs 难过 0.00，d′ = 0.64，分布区间重叠，不可用（尖峰被均值摊平）；
- 单点峰值口径：谢谢 208 窗 12.2 ± 2.0（中位 12.00），难过 170 窗 4.9 ± 8.9（中位 0.00）。

阈值取 5 的依据：208 个「谢谢」窗口无一低于 5，故「峰值 < 5 ⇒ 非谢谢」方向在录制数据上零误判；阈值 3 与 5 表现一致，取 5 以保留一倍噪声余量。

反方向存在 25.9% 的「难过」窗口峰值亦 ≥5，故附加 **top-2 约束**：仅当模型 top-1 与 top-2 恰为该对类别时门限介入。设计原则为「不修正优于把正确结果改错」。

### 8.4 复合词重组

「你好」由「指向对方」与「竖拇指」两段构成，而**竖拇指手形在纯触觉特征上与「谢谢」一致**。部署模型为纯触觉（294 维，无视觉通道），无法观测手部空间位置，该两段在当前传感配置下不可分——属观测不足而非训练不足。同时滑动窗口推理不具备词边界概念，两段各自达到确认条件即输出两个词。

三条备选方案被显式列举与评估：更换九轴 IMU 或接入视觉（硬件变更）、重新采集 `hello` 类样本（方案最彻底，需重采）、**在输出侧按规则重组**（现行方案）。

规则代价经评估：规则为无条件触发，真实意图为「你」+「谢谢」两词连续时会被合并。评估结论为该词序在汉语中不构成合法语句，代价为零。代码中记录了新增规则前必须回答的同一问题。

仅支持两段合并。触发点复用既有的最近确认词引用（仅保留一个历史词），且当前不存在三段复合词——遵循「不提前泛化」。

与标签合并的耦合被显式处理：规则中书写原始词 id，匹配时两侧均经 `mergeLabel` 归一化，使同一规则在合并开启与关闭（对照实验）两种配置下均成立。

### 8.5 语句链路的两项顺序约束

- 仲裁必须使用 CTC 的**逐词帧区间**（`greedyDecodeSpans`），不得使用全句全局峰值。实测「好看」全段拇指峰值中位为 26，语句「你真好看」中若解出一个「难过」，全局峰值将其改判为「谢谢」，即在原本正确的位置引入错误。
- **先仲裁、后合并**。复合词规则匹配 `you → thank_you`；若先执行合并，「你 + 难过」这一组合不会进入规则匹配范围，目标合并永不触发。

后处理须将全部改动上报（仲裁位置与改判前后类别、当时的通道读数、生效判据类型；合并的复合词 id；被屏蔽表过滤的词）。不上报时线上表现为「模型漏解出一个词」，而实际执行改动的是规则表。

---

## 9. 评测方法与效力边界

### 9.1 数据泄漏防护

- 验证集按**整条录制**分组划分并按标签分层（`split_by_sequence` / `planTrainValSplit`）；
- **验证集不施加任何数据增强**。此前的实现将「原始 + 增强副本」打乱后交由 `validationSplit` 截取尾部，在小样本条件下几乎每条验证样本均可在训练集中找到同源副本；
- 合成语句**先划分词样本池、再分别合成训练与验证集**（§11）。顺序颠倒会使同一条孤立词录制的片段同时出现于两侧，构成最难察觉的一类泄漏；
- 类别表由**数据集实际标签**导出而非由句型模板导出，否则 softmax 输出下标会发生偏移。

### 9.2 指标选择

语句级仅采用 WER；蒸馏的验证集仅使用硬标签；逐帧准确率经论证判定为无效指标（§5.2）。

### 9.3 效力边界（主动声明）

- 合成验证集 WER 0.7% **仅验证训练与解码管道的正确性**，不可用于预测实际佩戴表现。真实连续手语中的协同发音（前一词尚未收手即开始向后一词过渡）在孤立词录制中**结构性不存在**；
- 句型模板 56 句中 15 句有真人数据；复数句型仅 1 条真实句型支撑，应按纯合成预期；
- 采集端写入 `segments` 的帧区间为**占位值**（每词均填整段）。CTC 仅读取标签顺序，该目标合法，但不可作为对齐结果使用；
- 合成语句接缝时长 `overlap_ms = (100, 250) ms` 为估计值，`signTransitions.ts` 即为从真人录制测量该量而实现；
- 验证划分按录制分组，**未按受试者或采集会话划分**，跨受试者、跨日期、跨环境表现未经评测。

---

## 10. 数据增强：分层设计与作用域隔离

`DEFAULT_AUGMENT = { timeWarp: 0.15, cropMin: 0.85, amplitudeScale: 0.1, mountJitterDeg: 5, noise: 0.01 }`

| 层级 | 变换 | 作用域限定依据 |
| --- | --- | --- |
| 时间栅格 | 单调三节点分段线性时间弯折 + 随机裁剪 | 模拟动作速度差异；单调性保证动作时序不被打乱 |
| 特征幅度 | 幅度缩放 + 高斯噪声，**仅作用于触觉分量** `feat[:, :294]` | 关键点为几何量，对其缩放等价于改变手部物理尺寸，非合法增强 |
| 安装姿态 | 随机小角四元数**右乘**至每帧姿态 | 对应「每次佩戴角度不同」这一真实分布偏移来源 |

两项非显然的实现决策：

**裁剪顺序**。`buildSequenceFeatures` 中，起手段裁剪在增强裁剪**之后**执行，实现方式为将时间栅格压缩至有效区间 `[a,b]`。顺序颠倒（先裁剪有效段再随机裁剪）会使随机裁剪有概率删除动作起始段；现行顺序保证随机裁剪始终在手势内部取样。

**噪声的自相关结构**。`correlatedWalk(n, a, rng)` 实现 AR(1) 过程 `x_t = a·x_{t−1} + (1 − a)·ε_t` 并归一化至单位方差。采用白噪声不可行：白噪声一阶差分的自相关恒为 −0.5，模型会以该统计特征作为**捷径**判别静态类别。该决策直接基于「模型实际学到什么」的分析。

增强副本索引 0 恒为未增强原件。

---

## 11. 合成数据：伪影的识别、量化与消除

真人语句仅 90 条，其余 1680 条由孤立词录制经交叉淡化合成（`synth_sentences.py`）。

### 11.1 首版失效及其量化

初版实现将词样本首尾直接拼接。逐帧跳变量测结果：

| 位置 | 单帧跳变幅度 |
| --- | --- |
| 词边界处 | 0.0246 |
| 词内部 | 0.0002 |
| 比值 | **127×** |

模型习得的判别依据是「定位该跳变尖峰作为分词标记」。合成验证集 WER 4.4% 数值真实，但不具备任何外推效力——真人连续手语中不存在该尖峰。这是一例典型的**捷径学习（shortcut learning）**，其特征是在同分布验证集上完全不可见。

### 11.2 修正方案

1. 交叉淡化权重采用 `smoothstep`：`w(u) = u²(3 − 2u)`，在两个接点处 C¹ 连续；线性权重仍残留约 10× 的跳变尖峰；
2. 姿态四元数插值走 slerp，其余通道线性插值，整数通道按原精度取整；
3. **不插入静止段**。初版以冻结帧模拟词间停顿，该做法引入了另一类人工痕迹；
4. 仅在**模态签名一致**的录制之间淡化（`hand_signature`），避免拼接「含左手」与「不含左手」的录制；
5. 每条接缝的帧预算由 `_plan_overlaps` 分配，重叠不超过词长的 30%，且每词至少保留一帧自身数据。

### 11.3 带对照组的验收准则

定义 `seam_concentration` 为过渡段内逐帧跳变的 max/mean 之比；`control_concentration` 为在**真人录制内部**以同等长度滑动窗口计算的同一统计量。在 `overlap_ms=(100,250)`、`trim_words=True` 配置下（378/395 条样本被裁剪，接缝长度中位 8 帧）：

| 组别 | 中位数 | 高分位 |
| --- | --- | --- |
| 合成接缝 | 3.25 | 最大 6.80 |
| 真人录制对照 | 1.89 | p99 = 3.66（最大 9.00） |

接缝集中度相对对照组 p99 的比值在三种配置下均为 **0.86 ~ 0.89**，即合成接缝的不连续性已落入真实数据自身的波动范围内。

`__main__` 自检**在线测量对照组**后再执行断言（`c_p99 < ceiling × 0.85` 且 `med < round(0.5 × (c_p99 + ceiling), 1)`），不硬编码阈值。数据集变更时判据自动随之调整。

### 11.4 先验分布控制

句型模板中**有意加入一组无主语句型**以抑制代词先验。`_report_prior` 在任一词占比超过 35% 时告警——先验失衡在 WER 指标上不可见，属静默失效。

---

## 12. 时间包络：一类仅在实机佩戴时暴露的错误

所有样本最终重采样至定长 T。因此采集端多录入的一段静止会使每个词在归一化时间轴上的位置整体偏移。该错误在合成验证集与真实验证集上**均不可见**，仅在实机佩戴时表现为「识别结果普遍不准」。

一条语句样本的时间包络由三处协同确定：起点为 `armed` 状态下**首次判定为运动**的时刻（非按键时刻）；终点为剔除尾部实测静止后的位置；取数调用为 `snapshotAll(captureSpanMs(act), label, settleDropMs(act))`。

**后两个参数必须成对传递**。环形缓冲区间按尾部对齐，仅缩小捕获跨度将删除语句**起始**的若干词，尾部静止仍被保留。该错误可编译、可运行、结果表面正常。

工程处理方式为消除出错可能：`/translate` 与 `/collect-sentence` 共用 `sentenceEnvelope.ts` 同一模块（实现为普通类而非 Hook，以支持直接单元测试），使包络在构造上相同而非约定上相同。时间戳统一采用 `performance.now()`。

收句门限：单句模式 `SETTLE_MS` = 800 ms；连续模式基线 `CONTINUOUS_SETTLE_MS` = 1200 ms，并按本句实测最长停顿自适应抬高，上限 `SETTLE_CEIL_MS` = 3000 ms，使句中犹豫不致将一句切分为两句。`SENTENCE_MOTION_WINDOW_MS` = 600 ms 必须显著短于 2000 ms 的词窗，否则 800 ms 的尾部静止会被前序运动掩盖，语句将持续录制至 12 s 上限。此处选择在两端各接受约 600 ms 延迟，而非下调共享的 `IDLE_ENERGY`。

### 12.1 起手段裁剪：三层判据

录制流程为「按键 → 抬手入画 → 执行手势 → 结束」。抬手阶段的 0.5~1.5 s 会占用定长时间预算、污染参考帧（相对四元数首帧变为「手置于腿部」），并可能构成泄漏。

1. **可见性判据**：手部置于腿部时 MediaPipe 输出整帧 NaN，为最直接信号。**运动能量在此处是错误判据**——抬手本身即大幅运动，其能量高于多数手势。实现取双手可见性的**并集**（取交集在单手词上恒为假）、取**最长**连续段（非首段）、两端各保留 100 ms。
2. **到位判据**：可见性在手部进入画面瞬间即翻转为真，**画面内继续上抬的区段其无法观测**，而该区段最难处理（具备关键点、能量高、形态最接近手势）。判据采用手语研究中 transport phase 的标准做法——**速度谷底**，附加三道门限：须存在显著谷值、谷底位于前 60%、待裁剪段整体为向上冲程。速度按时间戳计算而非帧序号。手形常在抬手途中即已形成，故再附加一道**弯折钳位**，裁剪方向恒为保守（少裁）。
3. **触觉静止判据**：复用主手判定的能量口径与 `IDLE_ENERGY`，仅在存在真实弯折标定时启用，且为唯一会裁剪尾部的一层。

三项纪律：**判定失败则不裁剪**（六类 `reason` 分别记录，保留优于静默误裁）；**不写入数据库**（仅影响特征构建，判据变更后重训即可，亦可整体关闭）；**推理端显式不裁剪**（滑动窗口为纯触觉，可见性判据不成立，代码中显式写入 `trim: null` 以说明该事实）。

孤立词分支仅裁剪头部（`trimmed_view(head_only=True)`）：录制在最终姿态上保持冻结，而 2000 ms 推理窗口中即充满该冻结段，训练数据必须与之一致。两种裁剪变体的权重形状完全相同，故元数据中记录 `"trim": "none" | "head"` 以防混用。

### 12.2 以可视化作为硬性指标

`/train-sentence` 将每条真实语句渲染为一条裁剪带（`trimStrip.ts`）。硬性指标为 `uncoveredRuns`（完整落在保留区间之外的可见段数量），要求恒为 0。成因为第一层取最长可见段，语句中途 MediaPipe 丢失一次手部即将录制切分为两段、较短一段被整体丢弃（含第一个词）。实测 45 条真实语句中 13 条命中，最严重者 301 个可见帧仅保留 96 个。数值读数无法暴露该问题（`keptRatio` = 0.23 既可能来自两端等待过久，也可能来自前 4 秒动作被丢弃），渲染为带状图后可即时识别。

**该项改动的实测收益**：WER 由 6.3% 降至 4.5%，句首错误占比由 **100% 降至 49%**。根因为合成语句包含抬手段，且合成时接缝**永不落在首帧**（`lo = k[i-1] if i > 0 else 0`），致使首词的时间位置系统性偏移——这正是「错误集中于句首」这一异常表征的来源。

---

## 13. 左右手归一化：以等价变换替代重新采集

训练数据全部以右手采集，模型**在左手特征槽位上从未观测到任何信号**（特征层为两手各分配独立槽位，缺手段保持全零），且 137 维槽位顺序在左右手间呈镜像关系。佩戴左手套执行右手训练过的词时，输入落在训练分布之外，输出必然塌缩至某一固定类别。

手语中单手词换手不改变词义，故正确做法是将左手数据**归一化至右手口径**，**无需重新训练**：

- 137 维镜像置换表 `MIRROR_PERM_137` 由**两个独立来源互证**：其一为厂家规格书的 16×16 点阵字节映射（两手为精确 180° 旋转，1-based `byte n ↔ 257 − n`）；其二为同款手套第三方实现按**解剖学网格**编写的线序（同一映射在该坐标系下表现为「仅反转列、行序不变」）。137 个点逐点比对**零处不一致**，3 个虎口空位亦相互对应。
- 置换表性质已校验：双射、**自逆**（`perm[perm[k]] === k`，故左→右与右→左共用一表）、手指块映射至手指块、弯折通道 60~64 → `[64,63,62,61,60]`。掌部为不规则置换（5×15 减 3 格），手工推导必然出错，故必须为显式表；`handMirror.test.ts` 从 `sensorMapping.ts` 的索引表**重新推算整表**，索引表变更时立即失败。
- IMU 镜像为**反射共轭** `R′ = M R M`（`M = diag(−1, 1, 1)`）：四元数 `(w,x,y,z) → (w,x,−y,−z)`，真矢量 `(x,y,z) → (−x,y,z)`。仅需镜像每帧原始四元数，两个派生量自动满足：
  - 相对四元数：`(M R₀ M)⁻¹(M Rₜ M) = M (R₀⁻¹Rₜ) M`，即相对旋转的镜像；
  - 重力投影：世界重力无 x 分量，故 `g′ = M R⁻¹ M g = M g`，即重力矢量的镜像。
- **未经硬件验证项已标注**：镜面法向取 x 轴系由 `orientationCalib.ts` 的 `MODEL_MOTION_AXES` 反推（俯仰轴左右手同为 `[-1,0,0]`，偏摆轴左手 `[0,1,0]`、右手 `[0,-1,0]`，y 分量反号表明 x 垂直于镜面）。该推断需佩戴两只手套执行同一单手词、核验镜像后朝向通道是否重合方可确认。压力与弯折 137 维不受其影响。

### 13.1 主手自动判定

`dominantHand.ts` 从推理所用的同一滑动窗口判定，采用三路能量加权：`0.55 · 弯折 + 0.25 · 指压 + 0.2 · 朝向`。

- 弯折项取**标准差除以该通道两点标定量程**（实测左右手量程差异显著：左 51/121/138/135/62，右 40/99/176/120/74）；采用标准差而非极差以抗单帧尖峰；
- 朝向项取**相对首帧的最大偏离**而非累计路径（实测左手 IMU 抖动为右手的 4.5 倍：2621 vs 583 °/min），亦不采用净角度（手势存在往返）；
- `DOMINANCE_RATIO = 2.5`；**滞回为强制要求**（`FLIP_WINDOWS = 3`）：词执行中途翻转将产生「前半段右手系 + 后半段镜像」的输入，该输入在训练分布中不存在。双手能量均低于静止门限时**维持上一次判定**而非重新决策。

界面须显式提示「已按镜像归一化至右手口径」。静默执行该变换存在风险：识别结果异常时用户无法区分是手语动作错误还是软件将左手按右手处理。

采集端不执行镜像；持久化存储保留原始录制（已写入的镜像不可逆）。

---

## 14. 传感器健康监测：可检测量与不应检测量

`imuHealth.ts` 的判据不是「静止状态下的累计转角」，而是**四元数解算与加速度计的一致性**：

```
gExpected = gravityInHandFrame(q)      四元数推断的重力轴在手系中的方向
gMeasured = normalize(acc)             加速度计实测的比力方向
```

在静止或加速度以重力为主的条件下二者应当共线；四元数的俯仰/滚转一旦漂移，夹角即张开。该判据**不要求手部完全静止**，录制过程中的帧亦可筛选使用（仅统计 `|acc|` 落在 1g × (1 ± 0.15) 带内的帧）。阈值：夹角中位数 < 5° 判为健康，≥ 12° 判为不可用，其间为告警；可用帧不足 10 帧时输出 `unknown` 而非强行结论。

**偏航漂移不可检测，且不应检测**。六轴方案无磁力计，偏航绝对零点本不存在（此前实测的 −53.7° 「固定偏置」并非固定，而是上电瞬间的随机零点）。而特征层已仅使用对偏航不变的两个派生量，偏航漂移对识别无影响。代码中明确记录「不得为偏航添加告警，其只会产生假警报」。

陀螺零偏校准为**硬件动作**（短按各手套主控按键）。协议仅提供融合后的四元数、加速度与姿态角，**不提供原始角速度**，软件端无法执行零偏补偿，只能检测并指示需校准的手别。

采集流程另有一项要求：**两手均须执行弯折两点标定**。单路弯折传感失效会向 137 维中注入常量，训练过程不报错，仅表现为「某几个词始终学不好」。

---

## 15. 静默失效防御

以下五条测试各自锁定一类「改动后不会报错」的缺陷：

| 测试 | 锁定对象 | 缺失时的表现 |
| --- | --- | --- |
| `sequenceModel.test.ts`（单步真实 `fit`） | 空洞卷积反向传播缺陷 | 可构建、可 predict，`tsc`/`build` 全部通过，仅实际训练时失败 |
| `ctcDecode.test.ts`（Python logits 固定样本逐位对照） | JS/Python 两侧 blank 约定一致性 | 解码结果与唯一验收指标 WER 不一致 |
| `sentenceTemplates.test.ts`（实际解析 `synth_sentences.py` 逐条比对） | TS/Python 句型表漂移 | 前一版实现比对的是自行抄写的常量，无检出能力 |
| `handMirror.test.ts`（由 `sensorMapping.ts` 重算整张置换表） | 传感器索引表变更 | 镜像后手指错位，输出塌缩至固定类别 |
| `sentenceCapture.test.ts`（断言 `settleDropMs()` 与实际生效门限一致） | 时间包络不变性 | §12 所述「表面正常、实机全错」 |

当前测试规模：36 个文件 / 758 条断言。

同类工程决策：

- **仅导出权重而不使用 `tensorflowjs_converter`**。转换器无条件 `import tensorflow_decision_forests`，而 TF-DF 仅发布 Linux/macOS 轮子；绕过后仍受限于 tfjs 所用的 `tf_keras` 为 Keras 2，无法读取 Keras 3 格式。改为浏览器端自行构建同构结构并按层名装载权重（`modelWeights.ts`），可靠性反而更高：结构不同构会在**加载阶段**按层名与形状报错并指明具体层；而经转换器路线时，同类问题的表现为「加载成功、置信度正常、每个词都错」。
- **训练桥仅存在于开发环境**（挂载于 `configureServer`，`apply: "serve"`，`vite build` 产物中不含任何相关代码）。「接收文件 + 派生进程」置于生产服务器即构成远程代码执行面。两道防护缺一不可：请求来源须为回环地址，且须携带本次启动生成的随机令牌（因 `server.host: true`，开发服务器监听 0.0.0.0）。参数经白名单与数值校验，`argv` 数组交由 `spawn(shell: false)`，请求体中的字符串不进入任何命令行。
- **训练进度采用 JSONL 侧车文件**（`make_emitter`）：每行一条 `data` / `epoch` / `errors` / `done` 记录，启动时截断、逐行 flush，原有标准输出不作修改。前端可实时读取，终端行为不变。
- **IndexedDB 采用列式 TypedArray 存储**（DB v6，约 64 KB/条）。行式 `number[]` 存储会使 600 条样本膨胀至 400 MB 以上。视觉缺失帧填充 **NaN 而非 0**（0 为合法坐标值）。
- **统一 schema** `segments: [{label, startFrame, endFrame}]` 同时承载孤立词（长度 1）与语句（长度 = 词数），无需数据库迁移。

---

## 16. 能力对应关系

| 能力项 | 对应证据 |
| --- | --- |
| 序列建模与损失函数设计 | TCN 感受野解析计算（5 → 32 帧）；CTC 输出帧预算 `T/4 ≥ 2L−1` 的求解与启动期断言；`log_softmax(log p) = log p` 等价改写 |
| 表征学习与不变性先验 | 相对四元数 + 重力投影使偏航不变性由设计保证；关键点按手长归一化以分离手形与画面位置 |
| 迁移学习与知识蒸馏 | 跨模态蒸馏（视觉仅存在于训练期）+ 覆盖率门控；T=32 → T=128 骨干迁移作为 CTC 收敛前提 |
| 数据增强与分布偏移建模 | 三层增强与作用域隔离；AR(1) 相关噪声抑制白噪声捷径；安装姿态抖动对应真实域偏移来源 |
| 合成数据与捷径学习识别 | 127× 接缝尖峰的发现、量化与消除；以真实数据自身波动为对照组、不硬编码阈值的验收准则 |
| 评测方法论 | 按录制分组 + 标签分层划分；验证集不增强；WER 而非逐帧准确率；先划池后合成；主动界定 0.7% 的效力范围 |
| 数据泄漏与偏置识别 | 三条泄漏路径逐一封堵；先验失衡主动告警（阈值 35%）而非依赖 WER 发现 |
| 可识别性判定 | 偏航可行性探针给出 go/no-go 结论；三处「特征层不可分」的定量论证及对应的物理判据、规则与硬件路线 |
| 阈值工程 | 每项阈值均附阈值扫描表、有效样本量折算、通道失效条件下的鲁棒性对照、以及适用下界（`ROT_MIN_SPAN_MS`） |
| 端侧部署 | 浏览器端 tfjs 推理；Keras ↔ tfjs 逐层同构；按层名装载权重；MediaPipe 资源全本地化（CDN 请求 0 次） |
| 工程可靠性 | 五条静默失效防御测试；开发环境训练桥的双重安全约束；列式存储；不可逆操作不落库 |

---

## 17. 对外表述要点

### 17.1 简要版本（约 3 分钟）

> 该系统以一对压力/弯折/IMU 数据手套为输入，在浏览器端完成连续手语的整句解码与汉语语序重排。模型为 TCN + CTC，Keras 训练、TensorFlow.js 部署，两侧结构逐层同构。
>
> 项目的技术重点不在准确率数值。第一，采用跨模态知识蒸馏：摄像头仅在训练期作为教师模型输入，部署形态不依赖摄像头，以满足现场部署约束。第二,真人语句样本仅 90 条，其余 1680 条为合成数据，而首版合成方案存在结构性缺陷——词边界处的单帧跳变为词内部的 127 倍，模型据此建立了以跳变尖峰作为分词标记的捷径，合成集 WER 4.4% 不具备外推效力。我们量化了该伪影、以 smoothstep 交叉淡化消除，并设计了以真人录制自身波动为对照组、阈值在线测量的验收准则。第三，识别出若干类别在当前传感配置下属**观测不足而非训练不足**：例如「你」/「他」的差异仅为偏航角，而六轴 IMU 无绝对偏航，实测漂移率 15.92 °/s 而类间距仅约 45°，该方向不具可行性。相应处理为类别空间合并叠加物理判据后处理（四元数累计路径转角，正确率 97.7%，对照端点夹角判据的 71.0%），而非继续扩充数据。

### 17.2 完整版本（约 15 分钟）

建议顺序：§3 特征表示与偏航不变性 → §4 骨干网络与工具链约束 → §5 分类头、CTC 帧预算与骨干迁移 → §6 跨模态蒸馏 → §11 合成数据伪影的识别与消除 → §9 评测方法与效力边界 → §12 时间包络（面向工程落地方向的听众时该节信息量最高）。

### 17.3 深入问答要点

| 可能的问题 | 应答依据 |
| --- | --- |
| 如何确认模型未利用数据伪影 | §11.1 的 127× 尖峰分析、§10 的 AR(1) 噪声设计、§9.1 的三条泄漏路径 |
| WER 0.7% 是否过于乐观 | **应主动先行说明**其仅验证管道正确性；真实协同发音在孤立词录制中结构性不存在（§9.3） |
| 为何未采用 Transformer 架构 | 部署环境为浏览器端 tfjs；真人语句样本量 90 条；骨干需支持 T=32 → T=128 的长度迁移；当前瓶颈在数据与传感通道而非模型容量。可附述空洞卷积的前向/反向支持不一致问题 |
| 为何采用规则后处理而非端到端 | 三处「特征层不可分」的定量论证，以及每条规则代价的显式评估（§8.4） |
| 当前能否进入生产 | 不能，并明确差距：真人句型覆盖 15/56；按受试者划分的验证未执行；`_idle` 类需扩充至 40~60 条。**该项主动说明优于被动应答** |

---

## 18. 效力边界汇总

以下各项应与指标一并陈述。

**语句模型**

- 真人语句样本 90 条；56 句句型中 15 句有真实数据；复数句型基本为纯合成。
- 合成验证集 WER 0.7% 不预测实际佩戴表现。
- `segments` 帧区间为占位值，不可作为对齐结果使用。
- 合成接缝时长为估计值，尚无测量支撑。

**孤立词模型**

- 27 类中 `_idle` 仅 4 条、时长中位数 211 ms，实际由运动门控承担该职责。
- 「你好」/「谢谢」、「好看」/「像」、「你」/「他」在当前传感配置下特征层不可分。规则表可输出正确显示结果，但**不等同于模型完成了识别**。
- `is`（是）为孤儿类，每次训练均产生告警。

**硬件与特征**

- 加速度与姿态角 6 个槽位实测恒零，特征中 3 维加速度恒为 0。
- 六轴 IMU 无绝对偏航，上电零点随机，朝向仅可使用相对量。
- 手套**不具备位置观测**，而手语中的空间位置是语言学通道（额、颌、胸前对应不同词）。IMU 提供朝向不提供位置，加速度二次积分在手部无 ZUPT 可重置。故任何「人体执行手语」形态的可视化中，手部位置只能为常量。
- 陀螺校准为硬件动作，软件仅可检测并指示。

**遗留链路**

- 静态单帧链路的关键点为 MediaPipe 绝对图像坐标，未执行手腕平移与手长归一化，教师模型会将画面位置作为特征并经蒸馏传递。时序链路已绕开该问题。
- 骨架回归的目标为绝对坐标，而 z 为以手腕为原点的相对深度、常为负值，配合 sigmoid 输出层无法覆盖负值域，**深度通道失效**。2D 渲染仅绘制 x/y，故该缺陷在界面上不可见。

**评测**

- 验证划分按录制分组，非按受试者或采集会话划分；跨受试者、跨环境表现未经评测。
- 阈值的有效样本量需折算：转角速率的 348 个窗口来自 30 条录制，相邻窗口重叠 95%，有效样本量约 30。

---

## 19. 本项目所属的机器学习范畴与能力边界

对「这是否属于机器学习」这一问题，应给出明确且有界定的回答。

### 19.1 构成机器学习工作的部分

| 范畴 | 本项目中的具体内容 |
| --- | --- |
| 监督式深度学习完整训练流程 | TCN 骨干、两类分类头、交叉熵与 CTC 两种损失、`tf.GradientTape` 手写 `train_step`、按 WER 执行检查点保存与最优权重回载 |
| 序列建模与非平凡目标函数 | CTC 损失与贪心解码；输出帧预算 `T/4 ≥ 2L−1` 经解析求解并落为启动期断言。CTC 是语音识别与连续手语识别（CSLR）的标准方法，非封装调用 |
| 知识蒸馏 | 跨模态蒸馏（教师含视觉、学生纯触觉），温度缩放 T=3、软硬标签混合 α=0.5，附视觉覆盖率门控 |
| 迁移学习 | 孤立词学生模型骨干按层名与形状迁移至语句模型（T=32 → T=128），作为 CTC 收敛的必要条件 |
| 数据增强与分布偏移建模 | 三层增强与作用域隔离；AR(1) 相关噪声用于抑制白噪声捷径；安装姿态抖动对应真实佩戴域偏移 |
| 合成数据与捷径学习诊断 | 127× 接缝伪影的识别、量化、消除，及带活体对照组的验收准则 |
| 评测方法学 | 泄漏控制、分组分层划分、指标有效性论证（WER 而非逐帧准确率）、效力范围的主动界定 |
| 端侧部署 | 浏览器 tfjs 推理，Keras ↔ tfjs 逐层同构，按层名装载权重 |

其中**跨模态蒸馏、CTC 序列学习、捷径学习诊断**三项属于需要理解模型训练机理方能完成的工作，可作为区分「具备模型训练经验」与「仅调用现成接口」的实质证据。

### 19.2 应主动界定的边界

- **规模**：模型容量小（学生 c1=64 / c2=128，三个卷积块），数据量小（439 条词录制、90 条真人语句）。不涉及大规模训练、分布式训练、预训练模型或系统性超参搜索。
- **创新性**：骨干为标准 TCN，损失函数为现成的 CE 与 CTC，不含架构或目标函数层面的创新。
- **规则与模型的职责划分**：最终输出质量中有相当比例来自手工判据（转角速率、拇指仲裁、复合词重组、语序重排），而非模型本身。文档中已明确记录「规则表可输出正确显示结果，但不等同于模型完成了识别」。对外陈述时不得将规则的贡献计入模型能力。
- **生命周期完整性**：无线上部署规模、无 A/B 实验、无模型监控与迭代闭环。

### 19.3 建议的定位表述

> **面向传感器时序数据的应用型深度学习，及配套的机器学习系统方法学。**

该定位的支撑点在于对可识别性、指标效力与失效模式的判断能力——具体表现为能够区分「模型容量不足」与「观测信息缺失」，为每项阈值提供测量依据与有效样本量折算，并主动界定指标的外推范围。此类能力不随实验次数线性增长，属方法学层面。

若对标方向为大规模预训练模型或分布式训练体系，本项目不足以支撑该方向的经历陈述，应另行说明。

---

## 20. 相关基础概念与常见问询要点

本节为技术问答的知识准备，按「概念 → 要点 → 本项目对应位置」组织。标注「⚑」者为本项目可直接举出实例、建议优先展开的条目。

### 20.1 机器学习基础

**学习范式**

| 范式 | 定义要点 |
| --- | --- |
| 监督学习 | 有标注目标，学习 `x → y` 的映射。本项目两条链路均属此类 |
| 无监督学习 | 无标注，学习数据结构（聚类、降维、密度估计） |
| 自监督学习 | 从数据自身构造监督信号（掩码预测、对比学习），当前预训练模型的主流范式 |
| 强化学习 | 通过与环境交互获得奖励信号，优化策略 |
| 半监督 / 弱监督 | 少量标注 + 大量无标注；或标注粒度低于任务粒度。**CTC 属于弱监督的一种**：只给词序列，不给每个词的时间位置 ⚑ |

**偏差—方差与拟合状态**

- 欠拟合：训练误差与验证误差均高，模型容量或特征表达不足。
- 过拟合：训练误差低而验证误差高，模型拟合了训练集的噪声。
- 偏差—方差权衡：总误差 ≈ 偏差² + 方差 + 不可约噪声。增大容量降偏差升方差，正则化反之。
- 判别方法：绘制学习曲线（误差 vs 样本量）与训练曲线（误差 vs 轮次）。

**正则化手段**

| 手段 | 机理 |
| --- | --- |
| L2（权重衰减） | 惩罚权重范数，等价于对权重施加高斯先验 |
| L1 | 促使稀疏解，可用于特征选择 |
| Dropout | 训练时随机置零单元，近似模型集成；**推理时不启用**（inverted dropout 在训练期按 `1/(1−p)` 缩放） |
| 早停 | 以验证指标为准停止训练，本项目按 WER 保存最优权重 |
| 数据增强 | 在保持标签不变的前提下扩充输入分布 ⚑ §10 |
| 批归一化 | 兼具优化加速与轻度正则效果 |

**数据划分与泄漏** ⚑

- 训练 / 验证 / 测试三分。验证集用于模型选择与早停，测试集仅在最终评估时使用一次。
- K 折交叉验证：小样本下更稳定的泛化估计，代价是 K 倍训练开销。
- **数据泄漏**是本项目重点防护对象，常见形态：
  1. 同源样本跨集合分布（本项目按整条录制分组划分以避免）；
  2. 增强副本与原件分处两侧（本项目验证集不增强）；
  3. 预处理统计量在划分前计算（标准化的均值方差应仅由训练集估计）；
  4. 时序任务中用未来信息预测过去；
  5. 合成数据以同一原料生成后再划分（本项目先划分词池、后分别合成）。

**评价指标**

| 指标 | 适用与注意 |
| --- | --- |
| 准确率 | 类别均衡时可用；不平衡时具有误导性 |
| 精确率 / 召回率 / F1 | 精确率 = TP/(TP+FP)，召回率 = TP/(TP+FN)。代价不对称时须分别陈述 |
| 混淆矩阵 | 多分类的必要读数，能暴露特定类对的混淆——本项目正是据此定位「谢谢 / 难过」问题 |
| ROC-AUC | 对类别比例不敏感；正类极稀疏时过于乐观 |
| PR-AUC | 正类稀疏场景下比 ROC-AUC 更具区分力 |
| WER / 编辑距离 | 序列任务标准指标 ⚑ §5.2 |
| **指标有效性** | ⚑ 本项目的典型论证：逐帧准确率在 32 帧对 3~6 词的条件下，恒输出 blank 的退化模型即可达 85%，该指标无效 |

**优化基础**

- 梯度下降族：批量 / 随机 / 小批量。小批量在梯度噪声与硬件利用率间取平衡。
- 动量：累积历史梯度方向，抑制震荡、穿越窄谷。
- Adam：一阶矩（动量）与二阶矩（自适应步长）联合估计，附偏差修正。收敛快、对学习率相对不敏感，是默认选择。本项目 CTC 训练使用 `Adam(lr × 0.5)`——CTC 梯度稀疏，过大步长易发散。
- 学习率是影响最大的单个超参。常用策略：warmup、余弦退火、按验证指标衰减。
- 批量大小与学习率近似同向调整（线性缩放经验法则）。

**损失函数**

- 回归：MSE（对离群点敏感）、MAE、Huber。
- 分类：交叉熵。
- **为何分类不用 MSE** ⚑：配合 sigmoid/softmax 时，MSE 在输出饱和区梯度趋零，学习停滞；交叉熵对 logits 的梯度为 `(p − y)`，不随饱和消失。同时交叉熵是分类问题的极大似然解，属 proper scoring rule。
- 类别不平衡：类别权重、重采样、Focal Loss。
- 标签平滑：抑制过度自信，提升校准度。

### 20.2 深度学习基础

**反向传播**

- 本质是链式法则在计算图上的自动化应用，按拓扑逆序累积梯度。
- 前向保存中间激活以供反向使用，故显存占用随深度与批量增长；梯度检查点以计算换显存。

**激活函数与梯度问题**

| 函数 | 特性 |
| --- | --- |
| Sigmoid / Tanh | 两端饱和，深层堆叠导致**梯度消失** |
| ReLU | 正区间梯度恒为 1，缓解梯度消失；存在「神经元死亡」问题 |
| LeakyReLU / ELU / GELU | ReLU 的改良；GELU 为 Transformer 常用 |

- 梯度消失的缓解手段：ReLU 族激活、残差连接、归一化层、合理初始化、门控结构（LSTM/GRU）。
- 梯度爆炸的缓解手段：梯度裁剪、归一化、降低学习率。

**归一化层** ⚑（本项目每个卷积块后均接 BatchNorm）

- BatchNorm：按批次维度统计均值方差并归一化，附可学习的缩放与偏移。**训练与推理行为不同**——推理使用训练期累积的滑动统计量。小批量时统计量噪声大，效果劣化。
- LayerNorm：按特征维度统计，与批量大小无关，为 Transformer 与 RNN 的标准选择。
- 常见追问：卷积层为何 `use_bias=False`——后接 BatchNorm 的偏移参数会吸收偏置项，偏置冗余。本项目卷积层正是此配置。

**参数初始化**

- Xavier / Glorot：按 `fan_in + fan_out` 缩放，适配 sigmoid / tanh。
- He：按 `2 / fan_in` 缩放，适配 ReLU。本项目全部卷积与全连接层使用 `he_normal`，与 ReLU 激活一致。

**卷积网络**

- 核心性质：局部连接、参数共享、平移等变性。
- **感受野**计算 ⚑：逐层累积 `RF ← RF + (k − 1) × jump`，`jump ← jump × stride`。本项目骨干：5 → 6 → 14 → 16 → 32 帧，该数值在文档中给出并作为架构等价性的判定依据。
- padding：`same` 保持长度，`valid` 收缩。
- 池化：降采样、扩大感受野、提供局部平移不变性。
- **空洞卷积**：以扩张率在不增加参数、不降采样的前提下扩大感受野，为 TCN 的经典做法。⚑ 本项目因 tfjs 4.22 该算子反向传播不支持而改用「小核 + 池化」堆叠等价感受野（§4.1）。
- **全局平均池化（GAP）**：替代 Flatten + Dense，大幅减少参数、降低过拟合风险，并对时间位置不敏感。本项目孤立词头采用 GAP。
- 参数量：Conv1D 为 `k × C_in × C_out`（不含偏置），与序列长度无关——**这正是 T=32 权重可直接用于 T=128 的原因** ⚑。

**循环网络**

- RNN 的长程依赖受限于梯度消失。
- LSTM：遗忘门、输入门、输出门 + 细胞状态，以加性路径保持长程梯度。
- GRU：更新门、重置门，参数少于 LSTM，效果通常相当。
- 双向结构可利用未来信息，但不适用于流式推理。本项目 BiGRU 为可选分支。

**注意力与 Transformer**

- 自注意力：`softmax(QKᵀ/√d)V`，任意两位置一步可达，无长程衰减。`√d` 用于抑制点积随维度增长导致的 softmax 饱和。
- 多头：在不同子空间并行建模不同关系。
- 位置编码：自注意力本身置换不变，须显式注入位置信息（正弦 / 可学习 / RoPE）。
- 复杂度 `O(n²)`，长序列开销大；卷积为 `O(n)` 但感受野需堆叠获得。
- 残差连接 + LayerNorm 是可训练深层的前提。

**时序建模三类方案对比** ⚑（本项目为何选 TCN）

| 方案 | 优势 | 劣势 |
| --- | --- | --- |
| 1D CNN / TCN | 并行度高、推理快、参数少、感受野可解析计算、**与序列长度解耦** | 长程依赖需堆叠层数 |
| RNN / LSTM | 天然处理变长、状态可流式维护 | 训练不可并行、长程依赖仍受限 |
| Transformer | 长程建模最强、可扩展性最好 | 数据需求量大、`O(n²)`、端侧部署开销高 |

本项目选择依据：部署目标为浏览器端 tfjs；真人语句样本 90 条；骨干须支持 T=32 → T=128 的长度迁移。三项约束共同指向 TCN。

### 20.3 序列学习与 CTC 专题 ⚑

**CTC 解决的问题**：输入帧序列与输出标签序列长度不等且**无对齐标注**。

**机理**

1. 扩展标签集合，引入 `blank` 符号；
2. 定义「路径」为每帧一个符号的序列，路径经「折叠连续重复 → 删除 blank」映射到标签序列；
3. 损失为**所有可映射到目标标签的路径概率之和**取负对数，即对齐变量的边缘化；
4. 该和式以前向—后向动态规划在多项式时间内计算。

**关键性质与常见追问**

| 问题 | 要点 |
| --- | --- |
| blank 的作用 | 一为表示「当前帧无输出」，二为**分隔重复标签**——无 blank 则 `[a,a]` 无法与 `[a]` 区分 |
| 解码顺序为何是先折叠后去 blank | 顺序颠倒会使 `[a, blank, a]` 折叠为单个 a，重复词永久丢失（§5.2） |
| 长度约束 | 输出帧数须 ≥ `L + r`，`r` 为相邻重复标签数；最坏情形 `2L − 1`。本项目将其作为启动期断言 |
| CTC 的核心假设与局限 | **帧间条件独立**（给定输入，各帧输出独立），因此不建模输出侧的语言模型。实践中需外接语言模型或后处理弥补——本项目的语序重排规则即在此层 |
| 贪心解码 vs 束搜索 | 贪心取逐帧 argmax，快但非最优（最优路径 ≠ 最优标签序列，因多条路径映射到同一标签）。束搜索在标签序列空间上合并路径概率，通常更优，可融合语言模型 |
| 与 seq2seq + attention 的对比 | CTC 单调对齐、可流式、训练稳定；attention 解码器可建模输出依赖、不要求单调，但易产生重复或遗漏。语音领域常用 CTC + attention 联合训练 |
| 为何不看逐帧准确率 | blank 占绝对多数，退化模型即可获得高分（§5.2） |

**WER**：`Σ编辑距离 / Σ参考长度`，编辑距离含插入、删除、替换三类操作，由 Levenshtein 动态规划计算。可大于 1（插入过多时）。

### 20.4 迁移学习与知识蒸馏专题 ⚑

**迁移学习**

- 典型形态：预训练 → 微调。可选择冻结底层、仅训练顶层，或全量微调配合更小学习率。
- 适用前提：源任务与目标任务共享底层表征。本项目中孤立词与语句任务共享「手部动作的时序模式」这一底层表征。
- 与输入尺寸解耦是可迁移的结构前提：卷积与 BatchNorm 参数与序列长度无关。

**知识蒸馏**

- 基本形式：以教师模型的软化输出作为学生的监督信号。
- 温度 T：`softmax(z/T)`。T 越大分布越平滑，越能暴露非目标类之间的相对关系（即「暗知识」，dark knowledge）——这正是软标签优于硬标签的原因：硬标签只说「是 A」，软标签同时说明「更像 B 而不像 C」。
- 常见实现为两项损失加权；本项目采用**混合目标** `α·soft + (1−α)·hard` 后计算单次交叉熵。二者等价，因交叉熵对目标分布线性：`CE(αp + (1−α)y, q) = α·CE(p,q) + (1−α)·CE(y,q)`。
- 注意事项：若以两项损失实现，软标签项通常需乘 `T²` 以补偿温度对梯度幅度的缩放。
- 蒸馏的其他用途：模型压缩、集成压缩、跨架构迁移。

**跨模态蒸馏 / 特权信息学习** ⚑

- 教师使用推理阶段不可得的额外模态（本项目的视觉通道），学生仅用可得模态。该范式在文献中对应 **Learning Using Privileged Information（LUPI）**。
- 本项目的额外设计是**覆盖率门控**：教师输入质量不足时整体跳过蒸馏，而非按可用比例部分使用——避免将噪声作为知识传递。

### 20.5 泛化、分布偏移与数据质量 ⚑

**分布偏移的类型**

| 类型 | 定义 |
| --- | --- |
| 协变量偏移 | `P(x)` 变化而 `P(y|x)` 不变。本项目的佩戴角度差异、受试者差异属此类 |
| 标签偏移 | `P(y)` 变化而 `P(x|y)` 不变。对应本项目的先验失衡告警 |
| 概念漂移 | `P(y|x)` 随时间变化 |
| 域适应 / 域泛化 | 前者可见目标域无标注数据，后者完全不可见 |

**捷径学习（shortcut learning）** ⚑

- 定义：模型习得了与标签在训练集上相关、但不具备因果性或不可外推的特征。
- 本项目的两个实例：
  1. 合成语句词边界的 127× 跳变尖峰被用作分词标记（§11.1）；
  2. 白噪声一阶差分的固定自相关（−0.5）可被用于判别静态类别，故增强噪声改用 AR(1) 相关噪声（§10）。
- 识别方法：同分布验证集**通常无法暴露**捷径。需借助分布外测试、对特征分量的消融、输入归因分析，或如本项目所做的——直接量化伪影本身的统计特征并与真实数据对照。

**开集识别与 OOD 检测** ⚑

- 闭集分类器对任何输入都会输出某一类别，softmax 置信度**不是**可靠的 OOD 指标。
- 常见做法：置信度阈值（弱）、温度缩放 + ODIN、能量分数、Mahalanobis 距离、显式的拒识类、以及独立的前置门控。
- 本项目采用**独立物理门控**（运动能量门限）而非依赖模型置信度，依据是其核心观察：静止状态下常量输入的 softmax 分布最尖锐，置信度机制在此处系统性失效（§8.1）。

**合成数据与 sim2real 差距**

- 合成数据可解决样本量问题，但会引入源自生成过程的系统性偏差。
- 验收原则：**不应仅以合成验证集评估**，须显式界定其效力范围。本项目对 WER 0.7% 的界定即属此类（§9.3）。

**类别不平衡**：重采样、类别权重、Focal Loss、指标改用 PR-AUC 或按类召回率。本项目的「孤儿类」（`is`，单类样本极少）在每次训练时主动告警。

### 20.6 模型校准与可靠性

- 置信度校准：模型输出概率是否与实际正确率一致。深度网络普遍**过度自信**。
- 度量：可靠性图、期望校准误差（ECE）。
- 改善手段：温度缩放（后处理，不改变排序与准确率）、标签平滑。
- 与本项目的关联：确认出词依赖「连续 N 窗一致 + 置信度门限」，该机制的有效性直接受校准质量影响——这也是引入独立物理门控的另一层动因。

### 20.7 部署与工程

| 主题 | 要点 |
| --- | --- |
| 模型压缩 | 量化（INT8 训练后量化 / 量化感知训练）、剪枝（结构化 / 非结构化）、蒸馏 |
| 推理延迟 | 需区分吞吐与延迟。本项目为 100 ms 周期的实时推理，延迟为硬约束 |
| 端侧部署 | 浏览器 tfjs 后端选择（WebGL / WASM / CPU），静默降级须显式暴露——本项目以状态标识呈现 |
| 训练—推理一致性 ⚑ | 特征构建、归一化、时间包络必须两端同源。本项目通过共用同一模块使其**构造上一致**而非约定上一致（§12） |
| 可复现性 | 随机种子、数据版本、超参与环境记录 |
| 模型监控 | 输入分布漂移、预测分布漂移、性能衰减——本项目尚未建立，属已声明的缺口 |

### 20.8 大规模预训练模型相关（若被问及）

面向该方向的问询，建议以「了解概念、未在本项目中实践」的口径回答，并说明可迁移的部分。

- **Transformer 与自注意力**：见 §20.2。
- **预训练目标**：自回归语言建模（GPT 系）、掩码语言建模（BERT 系）、去噪自编码（T5 系）。
- **Scaling law**：模型规模、数据量与计算量之间的幂律关系，以及计算最优配比。
- **对齐流程**：监督微调（SFT）→ 偏好优化（RLHF / DPO）。
- **参数高效微调**：LoRA（低秩增量）、Adapter、Prefix Tuning，在显存与存储上显著优于全量微调。
- **RAG**：以检索结果作为上下文注入，用于知识更新与降低幻觉，与微调是互补而非替代关系。
- **推理侧**：KV 缓存、量化、投机解码、上下文长度与位置编码外推。

**可迁移点**：本项目在序列建模、弱监督对齐（CTC）、知识蒸馏、合成数据质量控制、评测效力界定这几项上的经验，与大模型方向的数据构建与评测环节直接相关——尤其是「指标证明了什么、不证明什么」以及「合成数据伪影识别」两项，在大模型的数据合成与评测基准建设中是同一类问题。

### 20.9 可能遭遇的追问及应答依据

| 追问 | 应答依据 |
| --- | --- |
| 97.7% 是怎么划分验证集得到的 | 按整条录制分组、按标签分层，验证集不增强（§9.1）。并主动补充：未按受试者划分，跨人表现未测 |
| 样本量这么小，结果可信吗 | 承认样本量限制；说明已采取的防护（分组划分、不增强、有效样本量折算），以及主动标注为「暂定值」的阈值 |
| 为什么用 CTC 而不做逐帧标注 | 逐帧对齐标注成本极高且主观；CTC 只需词序列即可训练，属弱监督。并说明本项目 `segments` 帧区间为占位值这一事实（§9.3） |
| 蒸馏真的带来提升了吗 | 应如实说明当前的证据形态与覆盖率门控的作用；若无消融实验，直接说明未做消融，而非给出无依据的数值 |
| 规则占了多少贡献 | 逐条给出：转角速率 97.7%、拇指判据 88.2%、复合词规则为无条件触发。并明确「规则正确不等于模型识别正确」（§19.2） |
| 如何判断是模型问题还是数据问题 | 举「你/他」的可识别性判定流程：先量漂移率与类间距，得出信息不在通道内的结论，再决定不通过训练解决（§3.1） |
| 下一步怎么做 | 补充真人语句（覆盖协同发音）、以 `signTransitions` 实测替换估计的接缝时长、扩充 `_idle`、按受试者划分验证集并报告混淆矩阵与延迟 |
