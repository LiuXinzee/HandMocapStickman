# 为 hand_mocap_stickman_2 添加动态手语词识别（时序模型）

> **状态：已实现**。下文是设计文档，实现与它有一处偏差（每帧维度 288/414 → **294/420**，
> 原因见「2. 特征层」里的引用块）。落地的文件：
>
> - 新建 `client/src/lib/sequenceFeatures.ts`、`sequenceModel.ts`、`sequenceWindow.ts`、
>   `client/src/hooks/useSequenceRecorder.ts`、`client/src/pages/SequenceCollect.tsx`（`/collect-seq`）、
>   `client/src/pages/TrainSequence.tsx`（`/train-seq`）
> - 新建 `python_train/`：`export_format.md`、`load_dataset.py`、`train_seq.py`、
>   `export_to_tfjs.py`、`requirements.txt`
> - 改 `datasetStore.ts`（DB v6 + `sequences` store）、`signLanguageVocab.ts`、
>   `Translate.tsx`（静态/序列双模式）、`App.tsx`、`Home.tsx`
> - 测试 43 个（`sequenceFeatures` 29 / `sequenceWindow` 8 / `sequenceExport` 6），全套 109 个通过
>
> 尚未做的：端到端实机验证（要接双手套 + 摄像头，见「验证」一节），以及句子级 CTC 的
> 完整训练流程（`train_seq.py` 里只有骨架，需要先录到多 segment 的连续手语数据）。

## Context

项目当前是**单帧静态分类**架构：

- `client/src/lib/datasetStore.ts` 的 `TrainingSample` 是一次性快照（左右手各 137 传感 + 4 四元数 + 21 关键点）
- `client/src/lib/signLanguageModel.ts` 是 408D→256→128→64 的 MLP 教师 + 282D 学生知识蒸馏
- `client/src/pages/Translate.tsx:100` 每 100ms 独立推理**一帧**，靠"连续 N 帧结果相同"做平滑

单帧看不到运动轨迹，所以「再见」（左右摆动）、「来」（招手）、「工作」（交替上下）这类带轨迹的词天生无解。`README.md:257` 已把这件事列为待办。

本次要做的是：把数据、模型、采集、训练、推理五层都升级到**时序**，并为将来的句子级（连续手语）留好接口。

### 已确认的设计决策

| 项 | 决定 | 理由 |
|---|---|---|
| 静态 vs 动态模型关系 | **统一为一个序列模型**，不做双模型+运动门控 | 门控阈值是不可靠中间环节：静态手势摆到位的过程、手抖都有运动，路由错了直接是错词且后续平滑救不回来。序列模型天然能表示"不动" |
| 旧静态样本 | **合成迁移 + 后续重录校准** | 旧数据不作废，能立刻跑通链路验证 |
| 训练输入 / 部署输入 | 训练用视觉+触觉融合教师，部署仅触觉学生（知识蒸馏） | 与现有静态模型策略一致 |
| 骨干网络 | **前期纯 TCN**，`backbone` 写成可切换参数，句子阶段再开 BiGRU | TCN 浏览器训练最快（全时间步并行）；TCN+BiGRU+CTC 是 CSLR 主流架构，backbone 到句子阶段不用换 |
| 句子级（CTC） | 本次只做孤立词，但**同时搭好 Python 训练出口** | 已验证 tfjs 4.22 **没有 CTC loss / CTC 解码器**（`node_modules/@tensorflow/tfjs/dist/tf.js` 里搜不到任何 ctc 符号），句子级训练无法原地在浏览器做 |

### 两个必须前瞻的设计（现在成本很低，后期省大事）

1. **backbone 与 head 分离**——时间维度的池化只放在 head 里，backbone 输出保持 `(T, C)` 不塌缩。句子阶段换成逐帧 head + CTC 即可。
2. **序列 schema 预留词边界**——存 `segments: [{label, startFrame, endFrame}]` 而非单个 `label`。孤立词=单 segment 覆盖全段，句子级同一 schema 直接装得下，不用做数据库迁移。同时保存**变长原始序列**，不只存重采样后的定长版本。

---

## 实施方案

### 1. 数据层 — `client/src/lib/datasetStore.ts`（DB v5 → v6）

新增 `sequences` object store。**旧 `samples` store 保持不动**（合成迁移要读它）。

采用**列存 + TypedArray**而非行存 object 数组：

```ts
export interface SequenceSegment {
  label: string;
  startFrame: number;  // inclusive
  endFrame: number;    // exclusive
}

export interface SequenceSample {
  id?: number;
  segments: SequenceSegment[];      // 孤立词 = 长度 1
  primaryLabel: string;             // 冗余字段，供 IndexedDB index
  frameCount: number;
  timestamps: Float32Array;         // [T] 相对录制起点 ms
  leftSensor: Uint8Array | null;    // [T*137] 0-255
  rightSensor: Uint8Array | null;
  leftImu: Float32Array | null;     // [T*10] = quat4 + acc3 + att3
  rightImu: Float32Array | null;
  leftLandmarks: Float32Array | null;  // [T*63]，视觉缺失帧填 NaN
  rightLandmarks: Float32Array | null;
  durationMs: number;
  sourceFps: number;
  origin: "recorded" | "synthesized";
  timestamp: number;
}
```

**为什么必须用 TypedArray**：一条 1.5s @ 100Hz 双手序列若用 `number[]` 存，每个 number 8 字节 → 约 700KB/条；30 词 × 20 条 = 420MB，IndexedDB 会撑爆。改用 Uint8（传感器本就是 0-255）+ Float32 + 录制栅格降到 50Hz（手语动作带宽远低于 25Hz），降到约 64KB/条、600 条共 38MB。

**为什么视觉缺失填 NaN 而不是 0**：0 是合法的关键点坐标，填 0 会和真实数据混淆。NaN 让预处理阶段能自由选择填充策略。

索引：`primaryLabel`、`timestamp`、`origin`。

CRUD 镜像现有静态样本的函数命名：`addSequence` / `getAllSequences` / `getSequencesByLabel` / `deleteSequence`（**单条删除必须有**——序列样本比静态样本贵得多，废样要能单独剔）/ `deleteSequencesByLabel` / `getSequenceStats`。

模型存储复用现有 `STORE_MODELS`，`SavedModel.modelType` 扩展为 `"fused" | "tactile" | "seq_fused" | "seq_tactile"`，并新增 `seqLen` / `backbone` / `frameDim` 元数据字段。新增 `getLatestSequenceModel()`（现有 `getLatestModel()` 保持原语义不动，避免破坏 Translate 的旧路径）。

### 2. 特征层 — 新建 `client/src/lib/sequenceFeatures.ts`

核心职责与几个关键的正确性处理：

**四元数相对化**——绝对朝向不可信（IMU 存在固定偏置、佩戴姿态因人因次而异）。每条序列用相对于**首帧**的相对四元数 `q_rel_t = q_0⁻¹ ⊗ q_t`。这既消除偏置，又正好对齐动态词的信息本质（信息在朝向**变化**里）。

> **实现时的改动**：原计划是"首帧绝对四元数单独保留 4D×2 拼进 head 前"，实际改成**每帧 3D 的重力方向在手系的投影** `R(q)⁻¹·[0,0,1]`。两个理由：① 首帧绝对四元数会让模型变成多输入，backbone/head 分离和句子级复用都要多绕一层；② 实测 IMU 的 yaw 有 −53.7° 固定偏置（见 memory `insole-fpa-dead-ends`），绝对四元数把这个偏置整个带进来，而重力投影对绕重力轴的旋转天然不变，**yaw 偏置照不进来**，同时又补回了相对四元数丢掉的绝对倾角信息。

**关键点归一化**——现有静态模型 `signLanguageModel.ts:76` 直接用原始 x/y/z，手在画面里的绝对位置会污染特征。但动态词的**手腕轨迹恰恰是关键信息**。所以拆成两部分：手腕点(landmark 0)保留绝对 3D 坐标（承载轨迹），其余 20 点相对手腕平移、再按手长（landmark 0→9 距离）归一（承载手型）。共 63D/手不变。

**视觉缺失插值**——单帧缺失沿时间轴线性插值补齐；整条序列该手都无视觉则 landmarks 全 0 并置 `visMask=0`。

**重采样**——`resampleSequence(sample, T)` 线性插值到定长 T（默认 32）。

每帧维度：

每手 147 维（`sequenceFeatures.ts` / `python_train/load_dataset.py` 两边一致）：

| 区间 | 内容 |
|---|---|
| `[0:137]` | 传感点 / 255 |
| `[137:141]` | 相对首帧四元数 `q₀⁻¹ ⊗ qₜ`（w,x,y,z） |
| `[141:144]` | 重力方向在手系的投影 `R(q)⁻¹·[0,0,1]` |
| `[144:147]` | 加速度 / 16，截断到 [-1,1] |

| 分支 | 组成 | 维度 |
|---|---|---|
| 学生（触觉） | 147 × 2 手 | **294D/帧** |
| 教师（融合） | 294 + 每手 63 关键点 × 2 | **420D/帧** |

（比原计划的 288/414 各多 6 维，来自上面那条重力投影的改动。单输入，无额外全局特征分支。）

**序列专属数据增强**（小样本下这是救命的，比噪声增强重要得多）：

- 时间扭曲：随机拉伸/压缩 0.8~1.25×（模拟打手语快慢差异）
- 时间平移 / 随机裁剪
- 传感器整体幅度缩放 0.9~1.1（模拟佩戴松紧，规格书标称 ±8%）
- 佩戴错位：四元数**右乘**一个随机小角度四元数 `q → q⊗m`（模拟手套相对手掌每次戴歪的角度不同）。
  必须在重采样后、构建特征前注入——相对四元数和重力投影是同一个 `q` 派生的，只改其中一块会得到物理上不自洽的帧。
- 高斯噪声（沿用现有 `augmentNoise` 参数）

时间扭曲/裁剪作用在**重采样栅格**上（`buildSequenceFeatures` 接受非均匀 grid），幅度缩放与噪声作用在
**特征层且只作用于触觉块**（关键点是几何量，乘增益等于把手凭空放大）。三者层次不同，不能合并。

### 3. 静态样本合成迁移 — `sequenceFeatures.ts` 内 `synthesizeFromStatic()`

把 `samples` store 里的单帧扩成 T 帧序列，注入**时间相关**抖动：

```
帧 t 的值 = 基帧 v₀ + 低通滤波(随机游走)ₜ × 小幅度
四元数：绕随机轴慢速摆动 ±2° 后重新归一化
```

**必须是低通相关噪声而非逐帧独立白噪声**。否则模型会学到"帧间差分是白噪声 ⇒ 静态词"这个捷径，真实推理时立刻崩。

产出的样本 `origin: "synthesized"`，UI 与统计中与真实录制样本分开计数。

> **已知风险**：合成序列的帧间统计与真实保持序列仍不完全一致。缓解手段是低通噪声 + 后续用真实录制的静态词序列做校准替换。采集页保留静态词的序列录制入口。

### 4. 模型层 — 新建 `client/src/lib/sequenceModel.ts`

结构镜像 `signLanguageModel.ts`，但用 functional API（`tf.model`）而非 `tf.sequential`，以支持 backbone/head 分离与多输入。

```
tcnBackbone(input (T, D)) → (T', C)     // T' = T/4，时间维不塌缩（塌缩是 head 的事）
  Conv1D(c1, k=5, padding=same) + BN + ReLU        // 感受野 5
  MaxPool1D(2)                                     // → 6，T/2
  Conv1D(c1, k=5, padding=same) + BN + ReLU        // → 14
  MaxPool1D(2)                                     // → 16，T/4
  Conv1D(c2, k=5, padding=same) + BN + ReLU        // → 32，盖满 T=32
  [backbone === "tcn_bigru" 时追加] Bidirectional(GRU(c2/2, returnSequences=true))

isolatedWordHead(x) → (N,)              // 孤立词，本次使用
  GlobalAveragePooling1D → Dense(d) + Dropout → Dense(N, softmax)

// 句子阶段预留：frameWiseHead(x) → (T', N+1) 逐帧输出 + CTC
```

通道数：教师 128/128/256 + Dense 128；学生 64/64/128 + Dense 64。

> **2026-08-13 修正：骨干原设计是空洞卷积（d=1/2/4，不加第二个池化），实机训练时抛
> `Error in gradient of conv2D: dilation rates greater than 1 are not yet supported in gradients`。**
> tfjs（4.22 实测）的 conv2D **前向支持空洞、反向不支持**，所以模型能建、能 predict，
> 一进 `fit` 就死 —— 这一页在浏览器里从来没训练成功过。改成"池化换空洞"后感受野
> 32 帧（原 30 帧）不变、参数 484k→550k、反而更快（时间维更早下采样）。
> `python_train/train_seq.py` 同步改成一样的结构（keras 支持空洞，但两边必须一一对应）。
> 代价：骨干输出分辨率 T/2 → T/4，句子级 CTC 逐帧输出粗一档（句子输入本身长得多，
> 比例上够用；且 tfjs 无 CTC，句子级只能走 python）。

导出：

```ts
export type SeqBackbone = "tcn" | "tcn_bigru";
buildSeqTeacher(numClasses, T, frameDim, backbone, lr): tf.LayersModel
buildSeqStudent(numClasses, T, frameDim, backbone, lr): tf.LayersModel
trainSequenceModel(samples, config, onProgress)   // 两阶段：融合教师 → 触觉学生蒸馏
predictSequence(window): PredictionResult | null
setActiveSequenceModel / loadSequenceModelFromSaved / serializeSequenceModel
```

序列化沿用现有 `tf.io.withSaveHandler` 方案（`signLanguageModel.ts:570`），但类型从 `tf.Sequential` 放宽到 `tf.LayersModel`。

**蒸馏实现直接沿用现有做法**（`signLanguageModel.ts:260-288, 438-442`）——即 `log → 除以 T → softmax` 生成软标签，再用 `α·soft + (1-α)·hard` 混合成单一 target 配 categoricalCrossentropy。这两步在数学上都是对的：前者因为 `log(pᵢ) = zᵢ - logsumexp(z)`，除以 T 后 softmax 里的常数项抵消，等价于对 logits 温度缩放；后者因为交叉熵对目标分布是线性的，标签混合等价于 `α·CE(soft) + (1-α)·CE(hard)`。会补一行注释说明，避免以后有人"修"坏它。

### 5. 采集层

**新建 `client/src/hooks/useSequenceRecorder.ts`**

- 通过 `useDualGloveSerial` 的 `onLeftFrame` / `onRightFrame` **全速回调**收帧（`useDualGloveSerial.ts:21-22`），**不要**轮询 `latestFrameRef`——轮询会丢帧
- 视觉走 `useHandTracking` 的 `handResultsRef`
- 内部维护左手/右手/视觉三路环形缓冲，各带 `performance.now()` 时间戳（与 `useSyncRecorder.ts` 同一时钟策略）
- 停止时按统一时间栅格（默认 50Hz）重采样：左右手套各取最近邻帧（复用 `useSyncRecorder.ts:186` `findNearest` 的游标优化思路），视觉取最近邻且时间差 <50ms，超出则标 NaN
- 产出 `SequenceSample`

**新建页面 `client/src/pages/SequenceCollect.tsx`**，路由 `/collect-seq`

沿用 `DataCollect.tsx` 的 Cyberpunk HUD 布局与设备连接卡片。差异：

- 录制模式：按住按钮/空格录制，松开停止；或固定时长倒计时（3-2-1 → REC 1.5s）
- 录完立即展示：帧数、时长、双手覆盖率、视觉覆盖率、**运动能量曲线**（确认真的录到动作了，而不是录了一段静止）
- 单条回放 / 单条删除
- 词汇列表复用 `SIGN_VOCABULARY`，按新增的 `dynamic` 标记分组显示

**不改造 `DataCollect.tsx`**——静态单帧采集流程仍被骨架回归（`TrainSkeleton.tsx`）依赖，保留原样。

### 6. 词汇表 — `client/src/lib/signLanguageVocab.ts`

`SignWord` 加 `dynamic?: boolean`。按现有 `description` 字段标注（"左右摆动"、"交替上下运动"、"招手"、"送向嘴边" 等为动态；数字类 0-9、"停"、"家"、"爱" 等为静态）。

**新增 `IDLE_LABEL = "_idle"` 伪类**（不进 `SIGN_VOCABULARY`，单独常量导出）。

> 这一条很关键：滑窗推理下，模型对**任何**窗口都会强行输出某个词。没有 idle 类，Translate 页面会在手放松、动作过渡、手在移动到起始位置时持续乱吐词。采集页必须提供"采集空闲/过渡段"的入口，建议 idle 样本数 ≈ 2~3 倍单词类样本数。

### 7. 训练层 — 新建 `client/src/pages/TrainSequence.tsx`，路由 `/train-seq`

沿用 `Train.tsx` 的布局与 `TrainingProgress` 展示组件。新增：

- 序列数据集统计：每类条数 + **真实/合成分别计数** + 平均时长
- 「从静态样本合成序列」按钮 → `synthesizeFromStatic()`
- 参数：T（默认 32）、epochs、batch、lr、蒸馏温度/α、**backbone 下拉（tcn / tcn_bigru，默认 tcn）**、时间扭曲强度
- 保存 `seq_teacher_*` / `seq_student_*` 两个模型
- 「导出数据集给 Python」按钮

### 8. 推理层 — 改造 `client/src/pages/Translate.tsx`

- 新增滑动窗口环形缓冲，由 `onLeftFrame`/`onRightFrame` 全速写入（保存最近 3s）
- 每 100ms 取最近 1.5s 窗口 → 重采样 T=32 → `predictSequence()`
- **保留现有的置信度阈值 + 平滑窗口 + 2 秒去重逻辑**（`Translate.tsx:128-160`）——这套逻辑对序列模型同样适用，不重写
- `_idle` 预测结果不进历史，仅重置平滑缓冲
- 顶部加模型模式切换（静态旧模型 / 序列新模型），便于 A/B 对比。旧路径完全保留

### 9. Python 训练出口 — 新建 `python_train/` 目录

**导出格式**（浏览器端生成，两个文件）：

- `dataset.bin` — 所有 TypedArray 顺序拼接的原始字节
- `dataset.json` — 索引清单：每条样本的 `segments`、`frameCount`、各数组的 `offset`/`length`/`dtype`、`origin`

不导出成纯 JSON 数组：600 条 × 64KB 二进制膨胀成 JSON 文本约 150MB，不可用。Python 侧 `np.frombuffer` 按 offset 切片即可。

**文件**：

- `export_format.md` — 二进制布局说明
- `load_dataset.py` — 读 bin+json → numpy
- `train_seq.py` — **用 tf.keras**（不是 PyTorch）实现与浏览器端**同结构**的 TCN(+BiGRU)，孤立词 CE 训练；预留 `--ctc` 分支骨架
- `export_to_tfjs.py` — `tensorflowjs_converter` 转回浏览器格式
- `requirements.txt` — tensorflow, tensorflowjs, numpy

**选 tf.keras 而非 PyTorch 的理由**：① 回导 tfjs 只要一行 `tensorflowjs_converter`，PyTorch 要绕 ONNX；② `tf.nn.ctc_loss` 是内置的，正是句子阶段需要的东西。

> 环境注意：本机全局 Python 是共享科研环境，这里**必须建独立 venv**安装（项目已有 `.venv/`，但那是给 `glove_bridge` 的，建议另建 `python_train/.venv`）。

### 10. 路由与导航

- `client/src/App.tsx` 加两条路由：`/collect-seq` → `SequenceCollect`、`/train-seq` → `TrainSequence`
- `client/src/pages/Home.tsx:280` 附近的导航网格加两张卡片（沿用现有 `cyber-panel` 卡片样式）

---

## 需要改动的文件

**新建**
- `client/src/lib/sequenceFeatures.ts` — 重采样、相对四元数、关键点归一化、增强、静态合成
- `client/src/lib/sequenceModel.ts` — TCN backbone / head / 两阶段蒸馏训练 / 推理
- `client/src/hooks/useSequenceRecorder.ts` — 多路同步序列录制
- `client/src/pages/SequenceCollect.tsx` — 序列采集页
- `client/src/pages/TrainSequence.tsx` — 序列训练页
- `client/src/lib/sequenceFeatures.test.ts`、`sequenceModel.test.ts` — vitest
- `python_train/` — 5 个文件（见上）

**修改**
- `client/src/lib/datasetStore.ts` — DB v6、`sequences` store、`SequenceSample` 类型、CRUD、`SavedModel.modelType` 扩展、`getLatestSequenceModel`
- `client/src/lib/signLanguageVocab.ts` — `dynamic` 字段、`IDLE_LABEL`
- `client/src/pages/Translate.tsx` — 滑窗推理 + 模型模式切换（旧路径保留）
- `client/src/App.tsx`、`client/src/pages/Home.tsx` — 路由与导航卡片

**不动**：`DataCollect.tsx`、`Train.tsx`、`signLanguageModel.ts`、`TrainSkeleton.tsx`、`VirtualMocap.tsx`、`gloveProtocol.ts`、`sensorMapping.ts`

---

## 验证

**单元测试**（`pnpm test`，项目已配 vitest，现有 5 个测试文件）
- `resampleSequence`：变长 → 定长 T，边界与线性插值正确性
- 相对四元数：`q₀⁻¹⊗q₀ = 单位四元数`；已知旋转序列的相对角度正确
- 关键点归一化：平移+尺度不变性；手腕绝对坐标被保留
- NaN 视觉插值：单帧缺失被补齐；整段缺失时 `visMask=0`
- `synthesizeFromStatic`：产出帧数正确，且**帧间差分的自相关显著高于白噪声**（这条直接验证捷径风险是否被规避）
- 二进制导出/导入 round-trip：TypedArray 逐字节相等
- 先跑一遍现有测试确保没回归

**端到端**（`pnpm dev`，需 Chrome/Edge + 两只手套 COM 口 + 摄像头）
1. `/train-seq` 点「从静态样本合成序列」→ 确认序列库出现 synthesized 条目
2. 直接训练一轮 → 验证整条链路能跑通、loss 下降
3. `/collect-seq` 连双手套+摄像头，录 3~4 个动态词各 ≥30 条 + `_idle` 若干 → 检查运动能量曲线确实有起伏、视觉覆盖率 >80%
4. `/train-seq` 重新训练 → 看教师/学生验证准确率
5. `/translate` 切到序列模型 → 实际打手语验证；重点看**手放松时是否稳定输出 idle 而不是乱吐词**
6. `/train-seq` 导出数据集 → `python_train/load_dataset.py` 能正确读回、形状与标签对得上

**回归确认**：`/collect`、`/train`、`/translate`（静态模式）、`/train-skeleton`、`/mocap` 全部功能不变。

---

## 风险清单

| 风险 | 影响 | 缓解 |
|---|---|---|
| 滑窗推理缺 idle 类 | 手放松时持续乱吐词，可用性归零 | 强制引入 `_idle` 伪类并在采集页给入口；idle 样本 2~3 倍于单词类 |
| 合成序列分布偏差 | 静态词在真实推理时准确率低于训练指标 | 低通相关噪声；后续用真实静态序列录制替换；统计页真实/合成分开计数以便追踪 |
| 每类样本量不足 | 动态词比静态词难学，20 条可能不够 | 建议每词 ≥30 条真实录制；重度依赖时间扭曲等序列增强 |
| IndexedDB 容量 | 存储撑爆 | 已用 TypedArray 列存 + 50Hz 栅格（约 64KB/条）；统计页显示估算占用 |
| tfjs 无 CTC | 句子级无法原地训练 | 已确认并绕开：backbone/head 分离 + schema 带 segments + Python(tf.keras) 训练出口 |
| BiGRU 浏览器训练慢 | 训练体验差 | 默认 `tcn`；`tcn_bigru` 仅作为可选项，主要留给句子阶段的 Python 训练 |

---

# 实施之后的追加轮次

> 上面是 2026-08-10 的原始计划，已全部实现。下面按时间顺序记录之后每一轮的改动、
> 为什么改、以及**踩过又不希望别人再踩一遍**的坑。IMU 自检 / 3D 手模 / 四步流程
> 那三轮在 `C:\Users\13500\.claude\plans\zesty-wishing-cascade.md`，这里不重复。

## 一、训练链路上的两个静默错误（已修）

### 1. 空洞卷积让整页训练无法启动

TCN 骨干原本是 `b2 dilationRate=2 / b3 dilationRate=4`。tfjs 4.22 的 conv2D
**前向支持空洞、反向不支持** —— 梯度算子里直接断言
`dilation rates greater than 1 are not yet supported in gradients`。
于是模型能建、能 `predict`，一进 `fit` 就抛（报错先出现在最后一层，即 `'1,4'`）。

改成**卷积 + 池化**堆叠，感受野按原始帧数（T=32）算：

```
b1 k=5  → 5    T=32
pool/2  → 6    T=16
b2 k=5  → 14   T=16
pool/2  → 16   T=8
b3 k=5  → 32   T=8
```

32 帧正好盖满整段，与原空洞版（30 帧）等效。代价是骨干输出时间分辨率从 T/2 降到 T/4：
孤立词后面接 GAP 无影响；将来句子级 CTC 逐帧输出会粗一档，但句子输入本身长得多，
比例上仍够，而且 tfjs 没有 CTC，句子级只能走 `python_train/`。

`sequenceModel.test.ts` 里加了一条**"反向传播能跑通"**的测试锁住这件事 ——
以后想改回空洞，先跑那条。

### 2. 验证集数据泄漏，准确率虚高

原写法是把 `原始 + 增强副本` 全部打乱成一个大数组，交给 tf 的 `validationSplit: 0.2`
切尾部。同一次录制的原始版可能落在训练集、它的增强副本落在验证集，而副本与原始只差
一点噪声 / 时间缩放 / 戴歪角度 —— 等于拿背过的题当考卷。小样本时尤其严重：
5 词 × 5 遍只有 25 条源样本，75 行里几乎每条验证行都能在训练集里找到自己的同胞。

改成 `planTrainValSplit`：**按源样本（整条录制）分组切**，整条要么全进训练、要么全进验证；
**验证集一律不做增强**（验证要衡量"没见过的一次真实动作"，增强过的验证集连分布都不对）。
按标签分层保证每类都有验证样本；类内只有 1 条时全部留给训练。与
`python_train/train_seq.py` 的 `split_by_sequence` 同策略。

验证集为空时（每类样本太少或 `validationSplit=0`）显式 `console.warn`，
因为那种情况下报出来的验证准确率是假的。

## 二、起手段自动裁剪 —— 新建 `client/src/lib/sequenceTrim.ts`

录制是「按下 → 抬手入画 → 打手势 → 结束」，抬手那 0.5~1.5s 也进了样本。伤害有三层：

1. **吃掉时间预算**。`resampleSequence` 把整条均匀重采样到 SEQ_LEN，栅格铺在整段上。
   抬手占 40% 就意味着真手势只剩 60% 的帧，而抬手时长每条都不同，同一个词于是落在
   不同的相位和时间尺度上。
2. **污染参考帧**。每帧四元数是相对**首帧**的（`firstValidQuatInv`），首帧是"手在腿上"，
   整条序列的朝向通道都在描述"相对腿上那一刻转了多少"。裁掉之后参考帧自动变成起势姿态。
3. **可能泄漏**。抬手快慢若与词相关（换词时手放下休息更久），模型可以拿它当捷径。

**判据只用视觉可见性，不用运动能量。** 手在腿上时 MediaPipe 根本看不到这只手，关键点整帧
是 NaN，这是最直接、几乎免费的信号。运动能量在这里恰好是**错的**判据 —— 抬手本身就是大幅
运动，能量比很多手势都高，IMU 通道更是全程在转；靠"动起来了"找起点会把起点定在抬手开始处，
等于什么都没裁。

算法（`detectSignSpan`）：

- 可见性取**双手的并集**。取交集会在单手词上永远为假（另一只手整条都不在画面里），
  那等于关掉裁剪。
- 找**最长**连续可见段，不是第一段 —— 抬手途中被摄像头扫到一两帧的误检不该定为起点。
  短于 `minRunFrames=3` 的段当噪声丢掉。
- 向外各留 `padMs=100` 的白：入画前一小段通常已经在起势，别切到手势本身。
- 换算成重采样栅格的归一化边界 `[a,b]` 时**必须按时间戳算**，不能按帧数比例
  —— `resampleSequence` 的栅格是归一化时间，按帧数比例会在丢帧的录制上偏掉。

**与增强的顺序**：增强的随机裁剪 `cropMin` 先在 `[0,1]` 上取好子区间，**再整体映射进 `[a,b]`**。
于是随机裁剪是在手势内部取子段，而不是有一定概率恰好裁掉起手段。反过来先压再增强会让两层
裁剪相乘，短样本被裁到只剩几帧。

三条守则：

- **判不出来就不裁**。无视觉 / 没有够长的可见段 / 裁完不足 6 帧或 250ms —— 每一种都有
  `reason`（`no_vision` / `no_run` / `too_short` / `full_span`），宁可留着起手段也不静默切错。
- **不写数据库**。只影响特征构建，`SequenceSample` 一个字节都不动，所以存量样本不用重录、
  判据改了重训一次即可，也随时能整体关掉（`trim: null`）。
- **推理端显式不裁**。`predictSequence` 拿到的滑窗是纯触觉（`sequenceWindow.ts` 把 landmarks
  写成 null），判据本来就不成立；那里显式传 `trim: null` 是为了**写明**这件事，免得以后有人
  给滑窗加上视觉时，推理行为跟着悄悄变。

裁剪是没有 UI 的自动预处理，所以 `summarizeTrim` 的统计行是它唯一的出口，训练完显示在
`/train-seq`：裁了几条、平均保留比例、各种没裁的原因各几条。没有这一行，"裁掉了什么"
在界面上完全不可见。

## 二之二、到位检测：切掉**画面内**那段抬手（2026-08-14）

上面那层只用可见性，实测下来暴露了它的结构性盲区：录制时手是从腿上抬起 → **进入画面** →
继续抬到起势位置 → 才开始打手势。可见性在"进入画面"那一刻就翻成 true，**画面内的那段抬手
它天生看不见**。而且这段更难对付：它就在画面里、有关键点、运动能量很高 —— 长得最像手势。
手全程都在画面里时，第一层甚至会判 `full_span`，一帧都不裁。

所以在可见段内部再加一层 `detectArrival`。判据用手语研究里 transport / preparation phase
的标准做法：**速度谷底**。把手送到起势位置是一次弹道式冲程，速度先冲到峰值、到位时落下来，
谷底就是手势起点。**仍然不能用运动能量**（理由同上，抬手本身能量就高）。

`detectHandArrival` 三道门限，缺一不可：

1. **必须有明显的谷** —— 平滑速度落到峰值的 `settleRatio=0.35` 以下。一直在动说明切不出边界。
2. **谷底必须落在可见段前 `maxArrivalRatio=0.6`**，再靠后就是在切手势本身了。
3. **要切掉的那一段整体必须是向上的冲程** —— `dy ≤ −0.08`（归一化图像单位，y 向下为正，
   抬起来是 y 变小）且 `|dy| ≥ 1.5·|dx|`。没有这一条，"再见"的第一个横向摆幅会被当成抬手切掉。

**门限 3 的位移必须量到谷底、不能量到速度峰值。** 匀速上抬时速度峰值就落在第一帧，量到峰值
得到的位移是 0，判据会把每一次标准抬手都否掉 —— 写这一层时第一版就是这么错的，所以三道门限
的**求值顺序**是 2→3→1，不是写下来的编号顺序。

配套两个细节：

- **速度按时间戳算，不按帧号。** 丢帧时按帧号算出来的速度是假的，会在丢帧处看到"速度不变"。
- **平滑窗口 3 帧**。30fps 下关键点抖动能造出假的速度谷。代价是检出点比真到位点晚约 1 帧
  （实测：真到位 25 → 检出 26，真到位 16 → 检出 17），这个滞后是稳定的、方向也安全（少切）。

**弯折钳位。** 手型经常在抬手途中就捏出来了；那一刻起画面里发生的事就可能已经属于手势，
起点不该再往后推。所以取 `min(到位帧, 弯折偏离初始手型 >25 ADC 的第一帧)`。
这是**钳位、不是与到位帧竞争的估计**，方向永远是"少切"。
137 维重排后弯折固定在下标 60~64；左右手这五路的手指顺序相反，但这里只求偏离幅度、
不关心是哪根手指，所以**不需要** canonical 化（`bendRange.ts` 那套反转在这里是多余的）。

**双手取较早到位的那只** —— 任一只手已经到位并开始做事，就不该再往后切。某只手算不出来
（没入画 / 没抬手）不构成否决，单手词的另一只手本来就整条不动。

**失败时退回第一层，不是退回整条。** 推后之后剩下不够 `minKeptFrames/minKeptMs` 时，
放弃第二层、保住第一层的结论。六种不采用的理由（`disabled` / `no_landmarks` / `no_rise` /
`no_settle` / `too_late` / `too_short`）都记在 `TrimSpan.arrival` 里。

**已知失效场景（写在这里免得以后当 bug 查）**：本身就以大幅上抬开头的词，速度没有谷底，
会走 `no_settle` 退回第一层 —— 不会切坏，但也帮不上忙。

统计单列 `arrivalApplied` / `meanArrivalDroppedMs` / `arrivalBendClamped`，在 `/train-seq`
显示成「其中 N 条另切掉画面内抬手平均 XXXms」。必须单列：一个"裁了几条"分不清两层各自
出了多少力，而这一层的**门限是要靠真数据调的**——`arrivalBendClamped` 占比高就说明手型
普遍在抬手途中成形，`no_settle` 多就说明 `settleRatio` 太严。

`sequenceTrim.test.ts` 从 19 条加到 33 条，锁住的关键行为：入画后继续上抬那段被切掉、
**手全程在画面里时从 full_span 变成裁得掉**、横向挥手判 no_rise、
上抬幅度不够/速度不落/谷底太靠后各自的退路、弯折钳位生效与不生效、
`arrival: null` 整层可关、退回时保住第一层结论、双手取较早、丢帧不影响判断。

---

## 三、界面上补的三处"看不见就查不出"

- **`TfBackendBadge`**（两个训练页）。tfjs 默认挑 `webgl` 后端，显卡驱动/上下文拿不到时会
  **静默**回落到 `cpu`，不报错、只是慢十几倍 —— 训练看起来"卡住不动"其实是在 CPU 上算。
  必须等 `tf.ready()` 再读 `getBackend()`，在那之前可能还没选好。
- **`/translate` 底部的双手 3D 手模**（`useHandModelDrive`，与 `/mocap` 同一份标定、同一套
  驱动函数，所以两页手型必然相同）。它不参与识别，是给"为什么不识别"提供第一手判据的：
  手模不动 = 手套没在出数据；手模动了但手型不像 = 弯折标定或某一路传感器的问题，跟模型无关。
  没有它的时候这两种硬件问题在这一页都表现为"模型不准"，会把人引向重训模型。
  这个 hook **全程不触发 React 重渲染**（手套 100Hz，走 state 会把整页每秒重渲染上百次），
  靠 ref + 各自的 rAF/`useFrame`。
- **自动加载播报两条链路**。`/translate` 以前只播报静态那条（`✓ 已自动加载静态模型 xxx`），
  时序模型是**默默**加载的 —— 于是"时序模型到底加载了没有"在界面上无从判断。现在两条各自
  独立加载、一起播报。

## 四、词表：`dynamic` 字段与 `_idle` 伪类

- `SignWord.dynamic?: boolean` 区分静态手型词与带轨迹的动态词。序列模型统一处理两者
  （静态词 = 帧间无变化的序列），这个字段**只用于**采集页分组显示和提示该录多久。
- `IDLE_LABEL = "_idle"` / `IDLE_DISPLAY_LABEL = "—"`。滑窗推理下模型对任意窗口都会强行
  输出某个词；没有这一类，手放松、动作过渡、手移到起始位置时会持续乱吐词。采集时要专门录，
  建议样本数为单词类的 2~3 倍；推理命中它就丢弃、不进翻译历史。

## 五、视觉层：白手套识别 + 左右手互斥（2026-08-14）

同事仓库 `LiuXinzee/HandMocapStickman` 的 `feat: 优化白色手套骨架识别` 一轮，评估后
**选择性移植**，四项。

### 5.1 白手套判据 `lightRatio`（`gloveVision.ts`）

实物手套是白色的（规格书写"黑色面料"是错的），而 `classifyGloveSurface` 原有三条 `isGlove`
判据**每一条都要暗像素**。新增一条**或**分支，黑手套的数一个都没动：

```
isLightGlove = lightRatio >= 0.58 && (lightContrast >= 5 || sampleLightDeviation >= 4)
isGlove      = isMaterialGlove || isLightGlove
```

- `lightRatio` 数的是低彩度**亮**像素。`isLowChromaLight` 里带一条 `warmSkinTone` 排除
  （`r−b≥18 && r>g·1.07 && g>b·1.04 && chroma/max≥0.13`），这是裸手不被算成白手套的关键。
- 光靠"亮"完全不够 —— **一面白墙 100% 满足**。所以配两道反假阳性闸门：
  `lightContrast`（手部包围盒平均亮度 − 外扩一圈环带的亮度）和 `sampleLightDeviation`
  （布料纹理的亮度标准差）。环带取包围盒外扩 `max(3, handSpan·0.07)` 再挖掉盒本身，
  不做形态学膨胀，够用且是 O(周长)。
- **手心/手背两条分支锁在 `isMaterialGlove` 之内**：银点是黑手套掌面的反光标记，白手套整只
  都是高亮低彩度，走那两条只会瞎猜。白手套一律 `surface="unknown"` —— 而 `surface` 本来就
  只进显示和元数据（`HandCanvas` / `HUDPanel` / `useSyncRecorder`），**从不进特征和模型**，
  朝向靠 IMU，所以这不是损失。

### 5.2 顺带修掉一个原有的白墙误判（`useHandTracking.ts`）

排查中发现的、**移植之前就存在**的 bug：`silverRatio` 的判据只要求
「亮 ≥130 + min ≥82 + 低彩度 ≤58」，一面白墙、一张白桌面、任何过曝区域都 100% 命中，
`silverRatio ≈ 1.0`。旧的置信度公式在 `!isGlove` 分支是 `(dark + silver − 0.2) × 0.8 = 0.64`，
**直接过 `≥0.25` 的闸门** —— 手套 ROI 恢复回路会拿白墙当手套区域反复重找手。

改成 `!isGlove` 时只给 `darkRatio` 的部分分。不会误伤真手套：`isGlove=false` 且
`dark+silver>0.51` 只在"银占绝对多数、暗很少"时可达，而 `dark≥0.42` 本来就会判成手套。

`isGlove=true` 分支用 `max(dark+silver, lightRatio)` 而**不是求和** —— 银色嵌片本身就是
亮+低彩度，会被 `lightRatio` 重复计一次；用 max 让黑手套的分数**逐位不变**，测试里锁了这条。

> 修正一处我自己先前的判断：白手套并不是"ROI 恢复回路永远不启动"，它一直在过闸门，
> 但是靠 `silverRatio` 蒙过去的。真正的缺陷是**闸门不挑食**，白手套和白墙一视同仁。
> 同事那份也有这个 bug（她两条分支都用了含 light 的 `materialRatio`），没有照抄。

### 5.3 左右手互斥约束 + 成对互换（`handIdentityTracker.ts`）

两只手同时在画面里，物理上必然是一左一右。原来每条 track 各自独立判手别，可以判出两个
`Left`，于是 `useSyncRecorder` 往 `leftLandmarks` 里写两只手、`rightLandmarks` 全是 NaN
—— **训练不报错、能量曲线正常**，只表现为某几个词学不好。

三段：

- `assignColdStartHandedness`：只在**一条 track 都没有**时按空间顺序定（送进检测的帧未镜像，
  镜像只在 `HandCanvas` 显示层 `ctx.scale(-1,1)`，所以图像左侧那只是用户的**右手**）。
  已有 track 时手别由时序证据接管，那时再按空间顺序改标签会在双手交叉的瞬间把两条一起搞反。
- `ensureUniqueHandednessPair`（照搬同事）：兜底的互斥约束，同标签时按 track 年龄让新的那条改。
- `tryPairHandednessSwap`（自己加的）：**只提高阈值是没用的** —— 有了
  `allowSwitch = tracks.length < 2` 加上年龄裁决，单条 track 的翻转在配对状态下永远活不下来，
  真交叉就变成不可恢复。所以真正的手别交换必须**成对**发生：两条都连续 30 帧互报对方
  （`PAIR_SWAP_FRAMES`，高于单手的 9 帧，因为代价不对称 —— 错换一次整条录制的左右都反了，
  不换只是半秒的 HUD 文字过期），且**两条的 `missedFrames` 都必须是 0**
  （丢检帧里 pending 计数是冻结的，拿它下结论等于用过期证据）。

### 5.4 MediaPipe 资源本地化 `client/public/vendor/mediapipe-hands/`

`hands.js` 与 `locateFile` 原本硬编码 jsdelivr，运行时要拉约 24MB。离线 / CDN 被墙 / 客户现场
演示时初始化失败，而失败的样子像是摄像头权限问题，很难往这上面想。

11 个文件已逐字节校对与 `cdn.jsdelivr.net/npm/@mediapipe/hands@0.4.1675469240` 一致（md5）。
两个坑：

- **`hands_solution_simd_wasm_bin.data` 是 0 字节但正常** —— 官方 CDN 的 `Content-Length`
  就是 0，`hands.js` 会请求它，删了会 404。
- 同事仓库里那 4 个 `.js` 与 CDN 对不上：内容相同、字节不同，是 Windows 检出时
  `core.autocrlf=true` 把 LF 换成了 CRLF。已用 CDN 原始字节，并加仓库根
  `.gitattributes`：`client/public/vendor/mediapipe-hands/** -text`。

**故意没移植**的：`classifySurfaceGeometry` 与 `handSurfaceStabilizer` 迟滞
（`surface` 只进显示、朝向靠 IMU，改它不产生任何数据收益）；她的 `roughness` 纹理混合
（0.75 的权重压过更可靠的几何线索）；`DETECTION_FRAME_WIDTH 512→640` /
`ANALYSIS_FRAME_WIDTH 320→416` / ROI `modelComplexity 0→1`（要先量 fps，单独一轮做）。

**反向该同步给同事的**：我们 `HandCanvas` 里 canvas 尺寸跟随 `video.videoWidth/videoHeight`
的修正，她那边没有。

### 验证

`npx tsc --noEmit` exit 0；`npx vitest run` **14 文件 / 234 测试**全绿（视觉层移植后 220，到位检测再 +14）；
`vite build ✓ 27.27s`；esbuild exit 0；11 个 vendor 文件进了 `dist/public`。

本地资源用 Playwright/Edge **把 jsdelivr 整个域名 DNS 黑洞掉**验证
（`--host-resolver-rules=MAP cdn.jsdelivr.net 127.0.0.1:1, MAP *.jsdelivr.net 127.0.0.1:1`，
配假摄像头）：`window.Hands` 挂载成功、13 次本地 vendor 命中（含两个 tflite 与 simd wasm）、
**CDN 请求 0 次**、无失败请求、无 pageerror。
注意 MediaPipe 是**懒加载**的，不点「启动摄像头」一个字节都不取 —— 脚本必须先点那个按钮，
否则会得到一个"什么都没发生"的假阴性。

**仍需插手套 + 真摄像头**：白手套在真实光照下能被认出来；双手手语过程中左右标签不互换。

## 六、换手：把左手数据镜像到右手口径（2026-08-14）

### 症状与根因

用户报的是「右手输入输出都正常，但戴左手套时永远输出『工作』」。这**不是 bug，是结构缺口**，
两件事叠出来的：

1. **特征层给两只手各留一段独立槽位**。`sequenceFeatures.ts` 的 `buildSequenceFeatures`
   左手写 `[0, HAND_TACTILE_FRAME_DIM)`、右手写 `[HAND_TACTILE_FRAME_DIM, …)`，
   `writeHandTactile` 开头 `if (!sensor && !imu) return` —— 缺手那段保持全 0，两只手之间
   没有任何共享。数据全用右手采，模型就**从没在左手那段槽位上见过任何信号**。
   一个训练集里不存在的输入，输出塌到某个固定类是必然的。
2. **137 维的槽位顺序左右手是镜像的**。`sensorMapping.ts` 里 `LEFT_HAND_INDEX_MAP` 是
   小指→拇指、`RIGHT_HAND_INDEX_MAP` 是拇指→小指。这两张表是**空间**顺序不是解剖顺序，
   所以即便见过左手信号，同一根物理手指也落在不同下标上。

**静态单帧那条路一模一样**：`signLanguageModel.predict` 的输入是
`[...handTactile(left), ...handTactile(right)]`，同样是两段独立槽位。所以两条链路都要修。

手语上单手词换手不改词义（换手只是惯用手不同），所以正解是**把左手归一化到右手口径**，
不是给每个词再采一遍左手。选的是「镜像函数 + 推理前归一化」，**不需要重训**。

### 137 维置换表从哪来（两个独立来源互证）

1. **规格书 V1.1 p10**「16x16 点阵的 256 压力数组解析方案」直接给出字节↔点阵关系
   `byte = (row−1)×16 + col`。把两只手的 137 个点铺上去，两只手是精确的 180° 旋转，
   即 1-based **`byte n ↔ 257−n`**（0-based `b ↔ 255−b`）。
2. **同款手套的另一份第三方实现**：
   `D:/JOB/projects/3_lnr/laonianren/back-end/code/python/app/hand/get_adc_form_csv.py`
   里的 `LeftHand` / `RightHand` 两个类，把线序写成
   **解剖学网格** —— 每指 5 行 × 3 列（第 5 行 `[-1, 47, -1]` 就是弯折传感器）、
   手掌 5 行 × 15 列、虎口 3 格写成 `-1`。137 点逐点比对**0 处不一致**，3 个空格的位置
   也互相落上。

在解剖学坐标里同一个映射表现为「**只反转列、行不动**」的左右镜像 —— 行序（指尖→指根）
左右一致，只有横跨手指宽度的 3 列换边，这正是解剖学上应有的样子。
（先前只看 16×16 字节网格时把它描述成 180° 旋转，是同一个置换的另一种坐标下的样子，
但要按镜像来理解。）

由此得到的 `MIRROR_PERM_137` 已核对：**双射**、**自逆**（`perm[perm[k]]===k`，
所以左→右和右→左是同一张表）、手指块仍落手指块 / 手掌块仍落手掌块、
弯折 60~64 → `[64,63,62,61,60]`、且每一槽都落在厂家网格里解剖学镜像的那一格上。
**手掌能精确镜像，不需要置零** —— 它是个不规则置换（5 行 × 15 列减掉 3 格），
手写「反转一下」一定会错，所以必须是显式表。

### IMU 怎么镜像

镜像一个旋转得到的是**反射共轭** `R' = M R M`（`M = diag(−1,1,1)`，镜面法向沿 x）：
四元数 `(w,x,y,z) → (w,x,−y,−z)`，真矢量（加速度、重力）`(x,y,z) → (−x,y,z)`。

只镜像**每帧原始四元数**就够了，特征层的两个派生量自动跟着对，这也是两条单测：
- 相对四元数 `q₀⁻¹⊗qₜ`：`(M R₀ M)⁻¹(M Rₜ M) = M (R₀⁻¹Rₜ) M`，正是相对旋转的镜像
  （即"镜像对四元数乘法是同态"）；
- `gravityInHandFrame(q)`：世界重力没有 x 分量，于是 `g' = M R⁻¹ M g = M g`，
  正好等于 `mirrorVec3(g)`。

`att`（姿态角）按同一规则取 `(roll, −pitch, −yaw)`，但**特征层完全没用到这三路**
（`writeHandTactile` 只取 quat 与 acc），写上只为让镜像后的样本整体自洽。

**未经硬件验证的一点**：镜面法向取 x 轴，是从 `orientationCalib.ts` 的 `MODEL_MOTION_AXES`
反推的 —— 俯仰轴左右手同为 `[-1,0,0]`，偏摆轴左手 `[0,1,0]` 右手 `[0,-1,0]`，y 翻号说明
x 垂直于镜面。要戴两只手套做同一个单手词、看镜像后的朝向通道是否重合才能定论。
压力/弯折那 137 维不受它影响（那部分是规格书 + 第三方实现双重印证的）。

### 落地

**新建** `client/src/lib/handMirror.ts` + `handMirror.test.ts`（25 条）：
`MIRROR_PERM_137`、`mirrorFrame137` / `mirrorSensorSeries`、`mirrorQuat` / `mirrorVec3` /
`mirrorImuSeries`、`mirrorHandInput`（静态单帧那条路）、`normalizeSingleHand`（时序滑窗）。
模块加载时断言表长 === `SEQ_SENSOR_N`：表是按 137 写死的，静默错位比崩掉危险得多。
测试里有一条**来源锁** —— 直接从 `sensorMapping.ts` 的两张索引表用 `255−b` 重算整张表并比对，
所以以后谁动了索引表会立刻红。

**改** `Translate.tsx`：两条推理路各套一层归一化。归一化状态只在**翻转的那一刻**进 state
（推理每 100ms 一次，无条件 setState 会把整页每秒重渲染十次），界面上显示一行紫色提示
「⇄ 只检测到左手套，已按镜像归一化到右手口径推理」—— **静默做这件事很危险**：
识别结果对不上时用户没法分辨是"手语做错了"还是"软件把左手当右手在算"。

**只在"只有左手"时动手**，三条边界写进了注释和测试：
- 两只手都有数据 → 可能是双手词，镜像会把两只手叠到同一槽位，纯粹的破坏。
  副作用是**两只手套都连着、只用左手做单手词时不会触发** —— 闲着那只手的槽位有静止数据，
  从数据上分不出"没戴"和"戴着不动"。要覆盖那种用法得在界面上让用户显式选惯用手。
- 只有右手 → 本来就是训练口径，原样返回（同一个对象，不做多余拷贝）。
- 视觉通道不碰。滑窗本来就是纯触觉两路都 null；关键点的镜像是另一套
  （图像 x 翻转 + 左右手标签互换），要用得单独写，不能顺手塞进来。

**改** `datasetStore.ts` + `TrainSequence.tsx`：`SequenceStats` 加 `handCounts`
（`leftOnly` / `rightOnly` / `both` / `neither`，判据用 sensor/imu 是否为 null，
与特征层"缺手全 0"同一口径），`/train-seq` 显示「手别分布 左 N · 右 N · 双 N」。
**这个问题之所以拖到用户实机才发现，就是因为页面上一直没有这个数** —— 总条数、每词条数、
视觉覆盖率全都正常。只在"确实有单手样本、且全偏在一侧"时告警，并说明单手词已由推理端
镜像兜住、双手词兜不住必须补采。

### 验证

`npx tsc --noEmit` exit 0；`npx vitest run` **15 文件 / 259 测试**全绿（新增 25 条）；
`vite build ✓ 14.35s`。

置换表的正确性是**离线核对**的（规格书网格 + 第三方实现 + 表自身的双射/自逆/分块不变量），
这部分不需要插手套。**仍需插手套**：只戴左手套做一个用右手训过的单手词，确认不再永远输出
同一个词；以及上面那条 IMU 镜面法向的验证。

